OPEN RECORD (2026-09-16): the `{{...}}` slots below wait on the first run of the hosted `gpu-linux-t4` lane, which was never provisioned. Committed as-is so the record travels with the package into graphty-monorepo; Phase M4 of `design/webgpu/plans/2026-09-16-graphty-monorepo-integration.md` fills and closes it there.

# G2 -- gate record for P2 (batch + dispatch infrastructure)

Phase P2 of `design/webgpu-acceleration-plan.md` (section 13, row P2) as
built against `docs/superpowers/plans/2026-09-14-webgpu-p0-p3-interfaces.md`
(the contract: section 7.3 rows P2-T1, P2-T2 and P2-T3, and the G2
checklist that follows them). Written by P2-T3 on 2026-09-15. Plain ASCII.

The rule of this record (spec 10.4): every number is measured and names
the command or the GitHub run it came from; a missed target is never
relaxed here -- it is re-fixed by an owner decision in section 6, or the
phase stays open. The same rule covers every tolerance of section 5.

Status of this record: sections 4, 5, 7 and 8 and the local half of
section 2 are filled from the dev-box runs of 2026-09-15; sections 1 and 3,
the GitHub half of section 2 and rows G2-9 / G2-10 keep their double-brace
template slots until the owner has pushed the P2 code commit (`tmp/commit-p2.sh
code`) and both lanes have run on it (P2-T3 Step 11) -- `tmp/commit-p2.sh
record` refuses to commit this file while a slot remains. The GPU lane's
runner `gpu-linux-t4` is not provisioned yet (the `gpu.yml` run of the
P0-T3 commit has been queued since its push), so those slots are an owner
item, not a measurement this task could take.

## 0. Gate status

| Item  | G2 checklist text (contract 7.3)                                                                                                                                                                                    | Proof                                  | Verdict                                                                                                                                                  |
| ----- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------- |
| G2-1  | `planUpload` unit tests for every path x caps table                                                                                                                                                                 | section 4                              | pass                                                                                                                                                     |
| G2-2  | the 1D / 2D boundary and the WGSL `linear_id` test                                                                                                                                                                  | section 4                              | pass                                                                                                                                                     |
| G2-3  | `reduce` and thread-per-row `segmentedReduce` equal their oracles at scaled sizes incl. all-equal keys and one hub row, both twins in-process, bitwise across two runs                                              | section 4                              | pass                                                                                                                                                     |
| G2-4  | the `UniformBlock` negative test rejected on Chromium and the state round trip green                                                                                                                                | section 4; decision G2-D1 in section 6 | pass (tests green on every adapter); the "rejected on Chromium" leg itself is NOT shown on the lanes' Chromium 153 and awaits the owner's decision G2-D1 |
| G2-5  | `E_DEVICE_LOST` mid-readback on both runtimes with recovery (`test/device/lost.test.ts` and `test/browser/lost.test.ts`)                                                                                            | section 4                              | pass                                                                                                                                                     |
| G2-6  | a bad bind group rejects its own batch's readback under Dawn-node                                                                                                                                                   | section 4                              | pass                                                                                                                                                     |
| G2-7  | leak counter 0 after release and dispose                                                                                                                                                                            | section 4                              | pass                                                                                                                                                     |
| G2-8  | the segmented-reduce sabotage rows fail their oracle test by >= 10x (`test/sabotage/segmented-reduce.test.ts`, `coverage.test.ts` green with "P2" listed) and its noise-floor rows recorded from the three adapters | sections 4 and 5                       | pass                                                                                                                                                     |
| G2-9  | the GPU lane green with `gpu-report.json` uploaded (`GRAPHTY_GPU_REQUIRE=nvidia`)                                                                                                                                   | sections 1 and 3                       | {{gpu-verdict}}                                                                                                                                          |
| G2-10 | the default lane <= 15 min (T-12; and the GPU lane <= 20 min)                                                                                                                                                       | section 2                              | {{ci-verdict}}                                                                                                                                           |

"pass" in rows G2-1 to G2-8 means `node tmp/g2/g2-map.mjs` (section 4)
and `node tmp/g2/check-noise-rows.mjs` (section 5) exited 0 on the runs
named there; this file was written only after they did. Rows G2-9 and
G2-10 are filled from the GitHub runs of the P2 code commit (section 1).

## 1. The commit and the runs

| Field                   | Value                                |
| ----------------------- | ------------------------------------ |
| P2 code commit          | `{{code-sha}}` -- "{{code-subject}}" |
| Default lane (`ci.yml`) | {{ci-run-line}}                      |
| GPU lane (`gpu.yml`)    | {{gpu-run-line}}                     |

Both runs were triggered by the push of the code commit to `master`
(`ci.yml` on `push: { branches: [master] }`, `gpu.yml` the same; contract
2.9). The GPU lane ran with `GRAPHTY_GPU_REQUIRE=nvidia`,
`GRAPHTY_BROWSER_GPU=nvidia` and `GRAPHTY_RUNNER_CLASS=gpu-linux-t4` (the
job `env` of gpu.yml).

State of `master` on GitHub when this record was written (2026-09-15):
`origin/master` is the P0-T3 commit `17f449b`; the P0-T4 record, the
housekeeping edits, the three P1 commits (`tmp/commit-p1.sh`) and the P2
code are all still in the owner's working tree. The only runs that exist
are those of `17f449b`: `ci.yml` run 34981584452 (#1, event push,
conclusion failure at `pnpm run build` after 0.57 min -- the P0 skeleton's
`src/browser/index.ts` imports `../context.js`, which P1-T1 creates, so
`master` does not build until the P1 commits land) and `gpu.yml` run
34981584430 (#1, event push, `changed` success, the `Test (NVIDIA T4,
GitHub-hosted)` job queued with no `gpu-linux-t4` runner to take it).
`tmp/g2/p1-ci-run.md` and `tmp/g2/p1-gpu-run.md` hold the
`lane-durations.mjs` output for both (exit 1 each).

## 2. Lane durations (T-12: default lane <= 15 min, GPU lane <= 20 min)

Source: `node tmp/g2/lane-durations.mjs ci.yml master {{code-sha-short}}`
and `node tmp/g2/lane-durations.mjs gpu.yml master {{code-sha-short}}`
(the GitHub REST job and step timestamps of the runs in section 1).

| Lane                                                                          | Job                               | Duration            | Budget | Result          |
| ----------------------------------------------------------------------------- | --------------------------------- | ------------------- | ------ | --------------- |
| default (`ci.yml`, ubuntu-latest: Dawn on lavapipe + Chromium on SwiftShader) | `Test (software adapters)`        | {{ci-job-min}} min  | 15 min | {{ci-verdict}}  |
| GPU (`gpu.yml`, runner `gpu-linux-t4`: Dawn and Chromium on the Tesla T4)     | `Test (NVIDIA T4, GitHub-hosted)` | {{gpu-job-min}} min | 20 min | {{gpu-verdict}} |

Default lane, job and steps (minutes, conclusion, step name):

{{ci-steps}}

GPU lane, jobs and steps:

{{gpu-steps}}

Local pre-check on the dev box (i9-14900, 32 CPUs, Mesa lavapipe
`llvmpipe: Mesa 23.2.1-1ubuntu3.1~22.04.3 (LLVM 15.0.7) 0.0.1`), `bash tmp/g2/time-default-lane.sh 5`
-- the same commands as the default lane in the same order and
environment (`tmp/g2/default-lane-local.txt`):

```
default lane mirrored locally on lavapipe
date: 2026-09-15T20:23:40Z  cpus: 32  node: v22.22.1  pnpm: 10.0.0
env: GRAPHTY_GPU_ADAPTER=llvmpipe GRAPHTY_GPU_REQUIRE=any VK_DRIVER_FILES=/usr/share/vulkan/icd.d/lvp_icd.x86_64.json XDG_RUNTIME_DIR=/tmp
build (pnpm run build)                    2 s  exit 0
lint (pnpm -r run lint)                  24 s  exit 0
knip (pnpm exec knip)                     3 s  exit 0
node --coverage (lavapipe)                4 s  exit 0
node no-subgroups pass (lavapipe)         2 s  exit 0
browser smoke (SwiftShader)              12 s  exit 0
test steps total: 18 s = 0.30 min
pre-check: 3 x 0.30 min + 5 min overhead = 5.90 min vs 15 min (T-12) -> PASS
steps exit status: 0 (0 = every step green)
```

The overhead argument 5 is spec 12.6's expectation, not a
measurement: no completed run of the P1 push exists (section 1), so the
job time outside the two test steps could not be read from GitHub. The
one run that exists (#1 of `17f449b`) measured its first six steps at
0.38 min (Set up job 0.03, checkout 0.02, pnpm/action-setup 0.03,
setup-node 0.02, `pnpm install --frozen-lockfile` 0.08, the lavapipe apt
step 0.20) before failing at the build step; build, lint, knip and the
Playwright cache steps are unmeasured on GitHub. Locally they took 2 + 24 +
3 s (the build reuses the tsc incremental state; a cold GitHub build is
longer).

Observed slowdown of ubuntu-latest against the dev box for the lavapipe
node step (both passes: coverage and no-subgroups): {{ci-node-step-min}}
min on GitHub / 0.10 min locally = {{slowdown-factor}}x (the
pre-check assumed 3x). P3-T7 uses
this factor for the P3 lavapipe budget (spec 13 row P3: the FA2 suite at
gpuScale sizes in <= 3 min).

### Run summaries (local runs, node tmp/g2/g2-map.mjs)

| Run                   | Files | Tests | Passed | Failed | Pending or skipped | Wall (s) |
| --------------------- | ----- | ----- | ------ | ------ | ------------------ | -------- |
| lavapipe              | 45    | 597   | 596    | 0      | 1                  | 2.6      |
| lavapipe/no-subgroups | 3     | 56    | 56     | 0      | 0                  | 0.6      |
| nvidia                | 45    | 597   | 596    | 0      | 1                  | 20.0     |
| swiftshader           | 8     | 30    | 30     | 0      | 0                  | 11.2     |
| chromium-nvidia       | 8     | 30    | 30     | 0      | 0                  | 2.5      |

Coverage of the lavapipe --coverage run (coverage/coverage-summary.json; thresholds 80 / 80 / 75 / 80): lines 96.5%, functions 98.83%, branches 96.08%, statements 96.5%

### Per-file durations of the lavapipe node run (slowest first)

| File                                       | Tests | Seconds |
| ------------------------------------------ | ----- | ------- |
| `test/kernel/linear-id.test.ts`            | 4     | 1.7     |
| `test/memory/upload-plan.test.ts`          | 53    | 1.5     |
| `test/primitives/reduce.test.ts`           | 12    | 1.5     |
| `test/kernel/wgsl-compile.test.ts`         | 13    | 1.1     |
| `test/primitives/segmented-reduce.test.ts` | 30    | 0.9     |
| `test/sabotage/reduce.test.ts`             | 7     | 0.7     |
| `test/memory/residency.test.ts`            | 22    | 0.6     |
| `test/sabotage/fa2-skeleton.test.ts`       | 9     | 0.6     |
| `test/layers.test.ts`                      | 11    | 0.6     |
| `test/benchmarks.test.ts`                  | 17    | 0.4     |
| `test/sabotage/segmented-reduce.test.ts`   | 8     | 0.3     |
| `test/device/context.test.ts`              | 36    | 0.3     |
| `test/algorithms/degree.test.ts`           | 14    | 0.3     |
| `test/kernel/determinism.test.ts`          | 3     | 0.2     |
| `test/build-output.test.ts`                | 16    | 0.2     |

Sum of per-file times: 13.0 s over 45 files (files run in parallel forks, so the wall time above is smaller).

## 3. The gpu-report.json upload

Artifact `{{gpu-artifact-name}}` of the GPU run in section 1, downloaded
with `gh run download {{gpu-run-id}} -n {{gpu-artifact-name}} -D
tmp/g2/{{gpu-artifact-name}}` and checked with `node
tmp/g2/check-gpu-report.mjs tmp/g2/{{gpu-artifact-name}}`:

{{gpu-report-summary}}

Pass criterion (contract 6.6 shape; spec 12.3 "fails loudly on a software
adapter"): `ok === true`, `policy.raw === "nvidia"`, `adapter.vendor ===
"nvidia"`, `adapter.software === false`, `runnerClass === "gpu-linux-t4"`,
the four `deviceLimits` present, `nvidiaSmi.available === true`,
`reason === null`. The `Adapter report` step of the run is green (the
script exits 2 on a policy violation and the step does not pipe through
`tee`), and the `graph-format GPU audit on NVIDIA (canary)` step greps
`adapter vendor=nvidia`.

Dry run of the checker (2026-09-15): no `gpu-results-<run id>` artifact
exists yet (no GPU-lane run has completed), so
`tmp/g2/check-gpu-report.mjs` was exercised on the dev-box report
`tmp/gpu-report-p0.json` (P0-T4's local `GRAPHTY_GPU_REQUIRE=nvidia`
run): seven checks pass, `runnerClass === "gpu-linux-t4"` fails as it must
for a dev-box report (`nvidia-lovelace-driver580`), exit 1.

## 4. Every G2 item mapped to the tests that proved it

The item text is the contract's G2 checklist (7.3) and, after it, the P2
deliverables of spec 13 row P2 the checklist does not name. The files are
the contract's 5.5 assignments. The test names are `ancestorTitles`
joined with " > " followed by `title`, both read from the vitest JSON
reporter's `assertionResults` for each file in the run named in the line
(its own `fullName` field joins with a space and is not used), extracted by
`node tmp/g2/g2-map.mjs` from the five local runs of P2-T3: `lavapipe` (node
project on Dawn lavapipe with `--coverage` -- the default lane's adapter
and command), `lavapipe/no-subgroups` (the lane's second pass over
`test/primitives test/layouts` with `GRAPHTY_GPU_NO_SUBGROUPS=1`), `nvidia`
(node project on the RTX 4070 SUPER -- the GPU lane's vendor),
`swiftshader` (browser project, Chromium on SwiftShader) and
`chromium-nvidia` (browser project, Chromium on the RTX 4070 SUPER with the
four Vulkan flags). "passed k/k" is the reporter's count; a file absent
from a run it belongs to, a file with zero tests, or a test with any other
status would have blocked this record (the script exits 1).

The one skipped test of the two full node runs (597 tests, 596 passed, 1
skipped) is `test/build-output.test.ts > dist (spec 2.5; hard failure
under CI, skip locally) > the tsc output and the bundle exist under CI`,
which skips by design off CI (G1 item 5 covers its CI form); it belongs to
no G2 file.

### G2-1 -- planUpload unit tests for every path x caps table (CAPS_TABLES, which gains CAPS_INTEL_XE at P2-T2)

- `test/memory/upload-plan.test.ts` -- lavapipe: passed 53/53 (1.5 s); nvidia: passed 53/53 (0.3 s)
    - arcsPerWindowFor > is floor(maxStorageBufferBindingSize / 4) rounded down to a multiple of 64
    - planArcWindows > returns no window for arcCount 0
    - planArcWindows > covers a small graph with one window ending at the last row (empty rows included)
    - planArcWindows > ends windows at row boundaries and aligns the next start down to 64 with `%`
    - planArcWindows > splits a hub row longer than a window into aligned chunks that share the row (PLAN DECISION 4)
    - planArcWindows > computes starts above 2^31 arcs exactly (a bitwise `& ~63` would give -2,147,483,520 for 2,147,483,800)
    - planArcWindows > rejects a bad arcsPerWindow, an empty rowPtr and an arcCount that disagrees with rowPtr
    - planArcWindows > partitions the arcs of every row exactly once, with aligned starts and bounded windows (fast-check)
    - planUpload on real snapshots > plans karate's arena hot prefix (880 B) by default: every segment inside the prefix is bound, the cold ones are null
    - planUpload on real snapshots > plans the full arena (2104 B) when a cold segment is needed and fits, listing every segment in the buffer
    - planUpload on real snapshots > binds a weights segment when the snapshot is weighted (hot prefix 1648 B)
    - planUpload on real snapshots > plans one buffer per needed present array on a fromCsr snapshot (arena === null)
    - planUpload on real snapshots > plans rowPtr only for arcCount 0 whatever `need` says
    - planUpload on real snapshots > never plans an identity permutation (a directed path): the cold names are null and the plan is the hot prefix
    - planUpload on real snapshots > always plans rowPtr even when `need` omits it, and is deterministic
    - planUpload on real snapshots > depends on the limits only: the subgroup facts and the software flag never change a plan
    - planUpload at the spec 4.2 tiers > the fake arenas reproduce the spec 4.2 / graph-format numbers
    - planUpload at the spec 4.2 tiers > 'spec-default' > 100k / 1M: the hot prefix (16,400,128 B) by default and the full arena (28,400,128 B) with a cold need
    - planUpload at the spec 4.2 tiers > 'spec-default' > 1M / 10M: the hot prefix (164,000,256 B) fits every table; the full arena (284,000,256 B) only the 4070
    - planUpload at the spec 4.2 tiers > 'spec-default' > 1M / 10M without an arena: one buffer per array on every table
    - planUpload at the spec 4.2 tiers > 'spec-default' > 10M / 100M: windowed at the 128 MiB binding (six windows, three buffers), arena / perArray on the 4070
    - planUpload at the spec 4.2 tiers > 'swiftshader' > 100k / 1M: the hot prefix (16,400,128 B) by default and the full arena (28,400,128 B) with a cold need
    - planUpload at the spec 4.2 tiers > 'swiftshader' > 1M / 10M: the hot prefix (164,000,256 B) fits every table; the full arena (284,000,256 B) only the 4070
    - planUpload at the spec 4.2 tiers > 'swiftshader' > 1M / 10M without an arena: one buffer per array on every table
    - planUpload at the spec 4.2 tiers > 'swiftshader' > 10M / 100M: windowed at the 128 MiB binding (six windows, three buffers), arena / perArray on the 4070
    - planUpload at the spec 4.2 tiers > 'lavapipe' > 100k / 1M: the hot prefix (16,400,128 B) by default and the full arena (28,400,128 B) with a cold need
    - planUpload at the spec 4.2 tiers > 'lavapipe' > 1M / 10M: the hot prefix (164,000,256 B) fits every table; the full arena (284,000,256 B) only the 4070
    - planUpload at the spec 4.2 tiers > 'lavapipe' > 1M / 10M without an arena: one buffer per array on every table
    - planUpload at the spec 4.2 tiers > 'lavapipe' > 10M / 100M: windowed at the 128 MiB binding (six windows, three buffers), arena / perArray on the 4070
    - planUpload at the spec 4.2 tiers > 'nvidia-4070' > 100k / 1M: the hot prefix (16,400,128 B) by default and the full arena (28,400,128 B) with a cold need
    - planUpload at the spec 4.2 tiers > 'nvidia-4070' > 1M / 10M: the hot prefix (164,000,256 B) fits every table; the full arena (284,000,256 B) only the 4070
    - planUpload at the spec 4.2 tiers > 'nvidia-4070' > 1M / 10M without an arena: one buffer per array on every table
    - planUpload at the spec 4.2 tiers > 'nvidia-4070' > 10M / 100M: windowed at the 128 MiB binding (six windows, three buffers), arena / perArray on the 4070
    - planUpload at the spec 4.2 tiers > 'intel-xe' > 100k / 1M: the hot prefix (16,400,128 B) by default and the full arena (28,400,128 B) with a cold need
    - planUpload at the spec 4.2 tiers > 'intel-xe' > 1M / 10M: the hot prefix (164,000,256 B) fits every table; the full arena (284,000,256 B) only the 4070
    - planUpload at the spec 4.2 tiers > 'intel-xe' > 1M / 10M without an arena: one buffer per array on every table
    - planUpload at the spec 4.2 tiers > 'intel-xe' > 10M / 100M: windowed at the 128 MiB binding (six windows, three buffers), arena / perArray on the 4070
    - planUpload at the spec 4.2 tiers > exceeds buffer, fits binding: the 1M / 10M arena at a faked 100,000,000 B maxBufferSize goes per array
    - planUpload at the spec 4.2 tiers > exceeds binding, fits buffer: the 1M / 10M colIdx at a faked 64 MiB binding is windowed into one buffer
    - planUpload at the spec 4.2 tiers > the 10M / 100M windows placed in one buffer keep offset = 4 x start
    - planUpload at the spec 4.2 tiers > exceeds binding on the 4070: a 2.4 GB colIdx over 100 hub rows is windowed at 536,870,848 arcs into one buffer
    - planUpload at the spec 4.2 tiers > exceeds buffer on the 4070: the full 10M / 100M arena above a faked 2,000,000,000 B maxBufferSize falls back to the hot prefix
    - planUpload at the spec 4.2 tiers > throws E_TOO_LARGE { path: 'rowPtr' } when even rowPtr exceeds the binding limit
    - planUpload at the spec 4.2 tiers > throws E_TOO_LARGE { path: 'binding' } when a needed edge-indexed array cannot be windowed
    - test/helpers/caps-tables.ts > ships five distinct tables and fakeCaps overrides limits and flags without touching the base
    - test/helpers/matchers.ts > expectAllClose: |a - e| <= abs + rel |e|, NaN pairs equal, the worst index reported
    - test/helpers/matchers.ts > expectBitwiseEqual: bytes, dtype and length; -0 differs from 0 and NaN payloads count
    - test/helpers/matchers.ts > flooredRelError: hand-computed floored per-node errors over stride-3 vectors
    - test/helpers/matchers.ts > maxRelError: max |a - e| / max(|e|, floor)
    - test/helpers/graphs.ts > generators: karate, grid, path, star, cycle, complete
    - test/helpers/graphs.ts > random generators are seeded and shaped as documented
    - test/helpers/graphs.ts > snapshotOf / csrSnapshotOf build the same graph on the arena and per-array paths
    - test/helpers/graphs.ts > every named fixture builds at scale 1 / 50 with the documented shape

### G2-2 -- the 1D / 2D boundary and the WGSL linear_id test (lavapipe in the default lane; the browser leg on SwiftShader and NVIDIA Chromium)

- `test/kernel/dispatch.test.ts` -- lavapipe: passed 14/14 (0.0 s); nvidia: passed 14/14 (0.0 s)
    - plan1d > MAX_1D_ITEMS is 65,535 x 256 = 16,776,960 (design 10.6), not 2^24
    - plan1d > 16,776,960 items are 1D and 16,776,961 are 2D with wg 256
    - plan1d > counts groups with ceil(items / wg)
    - plan1d > plan1d(0) is the empty plan { x: 0, y: 1 } (spec 5.6)
    - plan1d > the largest plannable count is 65,535^2 groups; one more group is E_TOO_LARGE
    - plan1d > rejects a negative or fractional item count and a workgroup size that is not a power of two
    - plan1d > gives the same plans from every capability table incl. a min != max subgroup table
    - plan1d > honours a faked per-dimension limit below the constant (the device limit is asserted equal at create())
    - plan2d > is plan1d's rule on a group count (spec 5.4): 1D up to 65,535 groups, then x = 65,535, y = ceil(groups / 65,535)
    - plan2d > agrees with plan1d on every group count: plan2d(ceil(items / wg)) is plan1d(items, wg) up to the items field
    - plan2d > is E_TOO_LARGE above 65,535^2 groups and E_INVALID_ARGUMENT for a bad count
    - groupsOf > is x * y
    - the P1-P3 stubs > planGridStride throws E_UNSUPPORTED { feature: 'planGridStride' } (lead f)
    - the P1-P3 stubs > planIndirect throws E_UNSUPPORTED { feature: 'planIndirect' } (lead f)
- `test/kernel/linear-id.test.ts` -- lavapipe: passed 4/4 (1.7 s); nvidia: passed 4/4 (2.6 s)
    - test/helpers/linear-id.ts (the constants the node and browser 17M-item tests share) > pins ITEMS = MAX_1D_ITEMS + 1, an odd VALUE, the samples across the boundary and the checksum's closed form
    - test/helpers/linear-id.ts (the constants the node and browser 17M-item tests share) > linearIdChecksum is the word sum modulo 2^32: hand-computed cases, wrap-around, and the full expected array
    - fill mode 1 over LINEAR_ID_ITEMS words: the 2D dispatch and the prelude's linear_id (spec 5.2, 11.5) > writes i + LINEAR_ID_VALUE at every sampled index, produces the pinned checksum, and is bitwise stable across two runs
    - fill mode 1 over LINEAR_ID_ITEMS words: the 2D dispatch and the prelude's linear_id (spec 5.2, 11.5) > the last word is the only item of the second workgroup row at WG = 256 (the boundary the test exists for)
- `test/browser/skeleton.test.ts` -- swiftshader: passed 5/5 (1.2 s); chromium-nvidia: passed 5/5 (0.7 s)
    - walking skeleton in the browser (spec 11.5, 11.6 item 2) > uploads the arena and the fromCsr paths and runs degree on karate, random1k and arcCount 0 against the oracle bitwise
    - walking skeleton in the browser (spec 11.5, 11.6 item 2) > fills the 17M-item map on the 2D dispatch: the sampled words equal i + value and the checksum is the pinned one
    - walking skeleton in the browser (spec 11.5, 11.6 item 2) > reduces the random1k f32 prefix sums through the subgroup twin against the f64 oracle, twice bitwise, and writes this adapter's fixture
    - walking skeleton in the browser (spec 11.5, 11.6 item 2) > runs one K3 + K4 exact-tile iteration on karate (paper mode) from host-written state: force vs the f64 pair sum and the trace vs the hand-computed controller, twice bitwise
    - walking skeleton in the browser (spec 11.5, 11.6 item 2) > adapterClassBrowser equals the Node adapterClass form on a faked GpuCaps

### G2-3 -- reduce and thread-per-row segmentedReduce equal their oracles at scaled sizes incl. all-equal keys and one hub row, both twins in-process, bitwise across two runs

- `test/primitives/reduce.test.ts` -- lavapipe: passed 12/12 (1.5 s); lavapipe/no-subgroups: passed 12/12 (0.6 s); nvidia: passed 12/12 (19.9 s)
    - reduce (spec 6 row 1): equals the f64 oracle, twins agree, twice bitwise > sum / f32 over sizes 0, 1, 255, 256, 257, 4097 and the scaled 65536 x 256 + 1
    - reduce (spec 6 row 1): equals the f64 oracle, twins agree, twice bitwise > sum / u32 over sizes 0, 1, 255, 256, 257, 4097 and the scaled 65536 x 256 + 1
    - reduce (spec 6 row 1): equals the f64 oracle, twins agree, twice bitwise > sum / vec4f over sizes 0, 1, 255, 256, 257, 4097 and the scaled 65536 x 256 + 1
    - reduce (spec 6 row 1): equals the f64 oracle, twins agree, twice bitwise > min / f32 over sizes 0, 1, 255, 256, 257, 4097 and the scaled 65536 x 256 + 1
    - reduce (spec 6 row 1): equals the f64 oracle, twins agree, twice bitwise > min / u32 over sizes 0, 1, 255, 256, 257, 4097 and the scaled 65536 x 256 + 1
    - reduce (spec 6 row 1): equals the f64 oracle, twins agree, twice bitwise > min / vec4f over sizes 0, 1, 255, 256, 257, 4097 and the scaled 65536 x 256 + 1
    - reduce (spec 6 row 1): equals the f64 oracle, twins agree, twice bitwise > max / f32 over sizes 0, 1, 255, 256, 257, 4097 and the scaled 65536 x 256 + 1
    - reduce (spec 6 row 1): equals the f64 oracle, twins agree, twice bitwise > max / u32 over sizes 0, 1, 255, 256, 257, 4097 and the scaled 65536 x 256 + 1
    - reduce (spec 6 row 1): equals the f64 oracle, twins agree, twice bitwise > max / vec4f over sizes 0, 1, 255, 256, 257, 4097 and the scaled 65536 x 256 + 1
    - reduce (spec 6 row 1): equals the f64 oracle, twins agree, twice bitwise > rejects a src smaller than count x element size, an out that cannot hold out[outOffset], and a bad count / outOffset
    - reduce (spec 6 row 1): equals the f64 oracle, twins agree, twice bitwise > writes the f32 sum over random1k as this adapter's noise fixture, records the twin and cross rows, and agrees with every committed adapter within the derived bound
    - reduce (spec 6 row 1): equals the f64 oracle, twins agree, twice bitwise > noiseFloorFor rejects an id without a committed floor (a tolerance without a floor is a finding)
- `test/primitives/segmented-reduce.test.ts` -- lavapipe: passed 30/30 (0.9 s); lavapipe/no-subgroups: passed 30/30 (0.3 s); nvidia: passed 30/30 (7.1 s)
    - segmentedReduceOracle (pure) > sum / min / max of the weights per row; the empty row gets the kernel's identity element
    - segmentedReduceOracle (pure) > v = 1 is the out-degree; row, arc and target are the CSR entries in arc order
    - segmentedReduceOracle (pure) > an unweighted snapshot passes weight 1; the fold is f64 (no f32 rounding of the sum)
    - segmentedReduceOracle (pure) > F32_MAX is the largest finite f32 (0x1.fffffep+127)
    - segmentedReduce thread-per-row (GPU) > n = 0: prepare succeeds, record records nothing, the result is empty
    - segmentedReduce thread-per-row (GPU) > matches the f64 oracle on one (sum / min / max x weight / one), twice bitwise
    - segmentedReduce thread-per-row (GPU) > matches the f64 oracle on self-loop (sum / min / max x weight / one), twice bitwise
    - segmentedReduce thread-per-row (GPU) > matches the f64 oracle on karate (sum / min / max x weight / one), twice bitwise
    - segmentedReduce thread-per-row (GPU) > matches the f64 oracle on grid10 (sum / min / max x weight / one), twice bitwise
    - segmentedReduce thread-per-row (GPU) > matches the f64 oracle on path1k (sum / min / max x weight / one), twice bitwise
    - segmentedReduce thread-per-row (GPU) > matches the f64 oracle on star200 (sum / min / max x weight / one), twice bitwise
    - segmentedReduce thread-per-row (GPU) > matches the f64 oracle on complete6 (sum / min / max x weight / one), twice bitwise
    - segmentedReduce thread-per-row (GPU) > matches the f64 oracle on random1k (sum / min / max x weight / one), twice bitwise
    - segmentedReduce thread-per-row (GPU) > matches the f64 oracle on hub10k (sum / min / max x weight / one), twice bitwise
    - segmentedReduce thread-per-row (GPU) > matches the f64 oracle on isolated (sum / min / max x weight / one), twice bitwise
    - segmentedReduce thread-per-row (GPU) > matches the f64 oracle on parallel (sum / min / max x weight / one), twice bitwise
    - segmentedReduce thread-per-row (GPU) > a directed snapshot walks the out-rows
    - segmentedReduce thread-per-row (GPU) > all-equal weights (0.75 on every arc): the sum is d x 0.75 within the bound, min = max = 0.75 exactly
    - segmentedReduce thread-per-row (GPU) > one hub row: a weighted star with 10k leaves (scaled)
    - segmentedReduce thread-per-row (GPU) > empty rows and arcCount 0 get the identity element: F32_MAX under min, -F32_MAX under max, 0 under sum
    - segmentedReduce thread-per-row (GPU) > row-count ladder 0, 1, 255, 256, 257, 4097, 65537 and 2^20 (scaled): weighted randoms vs the oracle
    - segmentedReduce thread-per-row (GPU) > the 1D / 2D dispatch boundary: MAX_1D_ITEMS + 1 rows (scaled) with no arcs -> every row F32_MAX under min
    - segmentedReduce thread-per-row (GPU) > accumulate = true combines into out: the sum doubles, min / max are unchanged
    - segmentedReduce thread-per-row (GPU) > USE_PERM is false in every P2 pipeline key; tiers !== null -> E_UNSUPPORTED { feature: segmentedReduce.tiers }
    - segmentedReduce thread-per-row (GPU) > a windowed core -> E_UNSUPPORTED; a core of the other weights pattern, a malformed rowPtr or a short out -> E_INVALID_ARGUMENT at record
    - segmentedReduce thread-per-row (GPU) > bad snippets -> E_SHADER_COMPILE { stage: compose, slot: VALUE }: target, a binding, a uniform, a foreign identifier, a flow keyword, no assignment to v
    - segmentedReduce thread-per-row (GPU) > good snippets beyond the two plain ones: a multi-statement conditional (keywords are not identifiers) and a comment naming forbidden words both run and match the oracle
    - segmentedReduce thread-per-row (GPU) > the sabotage check set passes on the real kernel with an error factor below 1
    - segmentedReduce thread-per-row (GPU) > twins in-process: the no-subgroup context gives bitwise the same result (TIER 0 calls no reduction helper)
    - segmentedReduce thread-per-row (GPU) > the weighted f32 sum over random1k is the cross-adapter noise fixture: within the derived floor of every committed adapter, rows recorded
- `test/kernel/determinism.test.ts` -- lavapipe: passed 3/3 (0.2 s); nvidia: passed 3/3 (7.0 s)
    - determinism (spec 11.9 item 4) > reduce f32 sum over 65,537 values: two runs bitwise on the subgroup twin and on the workgroup twin
    - determinism (spec 11.9 item 4) > segmented-reduce: two runs bitwise on both twins
    - determinism (spec 11.9 item 4) > the subgroup helper ranks its slots by key: the fold order is fixed whatever order the elected lanes took their slots

### G2-4 -- the UniformBlock negative test rejected on Chromium (accepted on Dawn-node, recorded) and the state round trip green on both runtimes

- `test/browser/uniform-layout.test.ts` -- swiftshader: passed 2/2 (0.0 s); chromium-nvidia: passed 2/2 (0.1 s)
    - uniform layout on Chromium: the hand-written misaligned struct follows uniform_buffer_standard_layout, the generated one compiles (spec 5.3, R-10) > generated offsets are strict
    - uniform layout on Chromium: the hand-written misaligned struct follows uniform_buffer_standard_layout, the generated one compiles (spec 5.3, R-10) > the hand-written struct is accepted iff uniform_buffer_standard_layout is exposed; the generated one always compiles
- `test/kernel/uniform-layout.test.ts` -- lavapipe: passed 2/2 (0.0 s); nvidia: passed 2/2 (2.1 s)
    - uniform layout: hand-written misaligned struct vs the generated one (spec 5.3, R-10) > the generated block has the strict offsets
    - uniform layout: hand-written misaligned struct vs the generated one (spec 5.3, R-10) > the generated struct compiles; the hand-written one is accepted iff uniform_buffer_standard_layout is present (Dawn-node: accepted)
- `test/kernel/state-roundtrip.test.ts` -- lavapipe: passed 2/2 (0.0 s); nvidia: passed 2/2 (2.8 s)
    - Fa2State round trip host -> kernel -> host (spec 5.3, 11.3) > FA2_STATE lays every field out at the 3.10.2 offsets inside 256 bytes
    - Fa2State round trip host -> kernel -> host (spec 5.3, 11.3) > every field round-trips through a one-workgroup kernel that adds 1; twice bitwise
- `test/browser/state-roundtrip.test.ts` -- swiftshader: passed 2/2 (0.0 s); chromium-nvidia: passed 2/2 (0.1 s)
    - Fa2State round trip in the browser (spec 5.3, 11.3) > FA2_STATE lays every field out at the 3.10.2 offsets inside 256 bytes
    - Fa2State round trip in the browser (spec 5.3, 11.3) > every field round-trips through a one-workgroup kernel that adds 1; twice bitwise

### G2-5 -- E_DEVICE_LOST mid-readback on both runtimes with recovery from a fresh adapter

- `test/device/lost.test.ts` -- lavapipe: passed 11/11 (0.1 s); nvidia: passed 11/11 (13.9 s)
    - PendingErrorSlot > starts empty, reports pending after set(), and take() returns then clears the error
    - PendingErrorSlot > chains a second and a third error as details.next of the first (the first is returned)
    - deviceLostError > is E_DEVICE_LOST { reason, message } naming both in the message
    - installUncapturedErrorSink > routes a broken bind group to onError as E_VALIDATION { label, message }; the uninstaller stops delivery
    - installUncapturedErrorSink > stores the error in the slot when onError is null; take() empties it
    - watchDeviceLost > resolves with the lost info after device.destroy() and calls onLost exactly once
    - device loss mid-readback and recovery (spec 5.7, 11.5) > device.destroy() while a read is pending: the read rejects E_DEVICE_LOST, the context is lost, the residency is cleared, no uncaptured error
    - device loss mid-readback and recovery (spec 5.7, 11.5) > a context over a FRESH adapter uploads, runs a degree kernel and reads back afterwards
    - P2: device loss mid-batch, the loss fan-out, the residency warning and residentBytes (spec 2.2 step 5, 5.7) > device.destroy() mid-batch rejects the batch readback with E_DEVICE_LOST; state lost; residency cleared; listeners ran once
    - P2: device loss mid-batch, the loss fan-out, the residency warning and residentBytes (spec 2.2 step 5, 5.7) > recovery: a new context from a FRESH adapter uploads and runs degree afterwards
    - P2: device loss mid-batch, the loss fan-out, the residency warning and residentBytes (spec 2.2 step 5, 5.7) > the residency warning fires once when more than warnUnreleasedSnapshots snapshots are resident
- `test/browser/lost.test.ts` -- swiftshader: passed 1/1 (0.1 s); chromium-nvidia: passed 1/1 (0.2 s)
    - device loss mid-batch in the browser (spec 5.7, 2.2 step 5) > device.destroy() mid-batch rejects the readback with E_DEVICE_LOST; lost state; residency cleared; listeners ran; recovery from a fresh adapter

### G2-6 -- a bad bind group rejects its own batch's readback under Dawn-node (thrown from the next assertReady() in the browser)

- `test/kernel/batch.test.ts` -- lavapipe: passed 6/6 (0.1 s); nvidia: passed 6/6 (12.4 s)
    - CommandBatch (spec 5.8) > a batch of 8 dispatches submits once; readback resolves the bytes of two requests at their offsets; dispatches count
    - CommandBatch (spec 5.8) > copy() moves bytes between bindings after ending the pass; bad lengths are E_INVALID_ARGUMENT
    - CommandBatch (spec 5.8) > submit twice / pass after submit / readback after submit -> E_INVALID_ARGUMENT
    - CommandBatch (spec 5.8) > discard() returns the slot after mapAsync and resolves an empty ArrayBuffer; two batches in flight hold two slots
    - CommandBatch (spec 5.8) > warm(): a batch after PipelineCache.warm dispatches without compiling
    - CommandBatch (spec 5.8) > a deliberately bad bind group rejects THE SAME batch's readback with E_VALIDATION { batchId, label } under Dawn-node
- `test/browser/batch.test.ts` -- swiftshader: passed 2/2 (0.0 s); chromium-nvidia: passed 2/2 (0.1 s)
    - CommandBatch in the browser (spec 5.8, 5.7) > a batch of 8 dispatches submits once; two readbacks land at their offsets; discard returns the slot
    - CommandBatch in the browser (spec 5.8, 5.7) > a deliberately bad bind group: rejected by its own batch or thrown from the next assertReady() after onSubmittedWorkDone()

### G2-7 -- leak counter 0 after release and dispose (and every Lease buffer back in the pool)

- `test/leak.test.ts` -- lavapipe: passed 3/3 (0.1 s); nvidia: passed 3/3 (5.2 s)
    - LeakCounter (contract 5.2) > counts createBuffer, distinct destroys and mapAsync on a raw device; restore() removes the proxies
    - LeakCounter (contract 5.2) > after degree + release + dispose on an adopted device, live === 0 (spec 11.5)
    - LeakCounter (contract 5.2) > degree makes exactly one mapAsync call per invocation
- `test/memory/lease.test.ts` -- lavapipe: passed 5/5 (0.0 s); nvidia: passed 5/5 (8.0 s)
    - Lease (spec 4.4) > the size classes the accounting below relies on
    - Lease (spec 4.4) > storage / uniform / acquire go back to the pool on release; count tracks the live buffers
    - Lease (spec 4.4) > releases in the try / finally pattern when the algorithm throws
    - Lease (spec 4.4) > E_DISPOSED after release; the pool's own errors pass through unchanged
    - Lease (spec 4.4) > release() after ctx.dispose() is silent: the pool already destroyed everything

### G2-8 -- the segmented-reduce sabotage rows fail their oracle test by >= 10x; coverage.test.ts green with P2 listed; the noise-floor rows (G2.md section 5)

- `test/sabotage/segmented-reduce.test.ts` -- lavapipe: passed 8/8 (0.3 s); nvidia: passed 8/8 (12.3 s)
    - sabotage: segmented-reduce > has at least three rows, each find unique in the normative body, each naming this task's primitive test, and P2 is a gated phase
    - sabotage: segmented-reduce > the real kernel passes the check set (factor < 1)
    - sabotage: segmented-reduce > last-row-skipped: fails the segmented-reduce check set by >= 10x
    - sabotage: segmented-reduce > weight-read-dropped: fails the segmented-reduce check set by >= 10x
    - sabotage: segmented-reduce > has-weights-inverted: fails the segmented-reduce check set by >= 10x
    - sabotage: segmented-reduce > min-identity-zero: fails the segmented-reduce check set by >= 10x
    - sabotage: segmented-reduce > empty-rows-skipped: fails the segmented-reduce check set by >= 10x
    - sabotage: segmented-reduce > perm-select-swapped: fails the segmented-reduce check set by >= 10x
- `test/sabotage/coverage.test.ts` -- lavapipe: passed 6/6 (0.0 s); nvidia: passed 6/6 (0.0 s)
    - sabotage coverage (spec 11.9 item 1, 13 rule f) > lists P1 and exempts exactly fill and fa2-to-scene
    - sabotage coverage (spec 11.9 item 1, 13 rule f) > every non-exempt kernel of a listed phase has >= 3 rows
    - sabotage coverage (spec 11.9 item 1, 13 rule f) > the P1 rows are the four degree, four reduce, four K3 and three K4 mutations, by name
    - sabotage coverage (spec 11.9 item 1, 13 rule f) > every find string occurs exactly once in the entry's normative body, the replacement differs, minFactor >= 10, names unique
    - sabotage coverage (spec 11.9 item 1, 13 rule f) > every test names an existing test file under test/
    - sabotage coverage (spec 11.9 item 1, 13 rule f) > sabotagedBody throws on an absent find and on a duplicated find
- `test/noise-floor.test.ts` -- lavapipe: passed 9/9 (0.1 s); nvidia: passed 9/9 (1.9 s)
    - noise floor (benchmarks/results/noise-floor.json) > this adapter's FA2 paper-leg output: bitwise across two runs, written under GRAPHTY_NOISE_FLOOR_WRITE=1, within the cross-adapter floors of every committed adapter output
    - noise floor (benchmarks/results/noise-floor.json) > degree / random1k: every committed output agrees (bitwise; rows none recorded here)
    - noise floor (benchmarks/results/noise-floor.json) > reduce / random1k: every committed output agrees (elementwise; rows none recorded here)
    - noise floor (benchmarks/results/noise-floor.json) > fa2-repulsion-exact / karate: every committed output agrees (floored-stride3; rows fa2-skeleton.force.cross, fa2-skeleton.force.twin, fa2-skeleton.force.oracle-f64)
    - noise floor (benchmarks/results/noise-floor.json) > fa2-speed-finalize / karate: every committed output agrees (elementwise; rows fa2-skeleton.trace.cross, fa2-skeleton.trace.twin, fa2-skeleton.trace.oracle-f64)
    - noise floor (benchmarks/results/noise-floor.json) > fa2-speed-finalize / networkx-karate: every committed output agrees (elementwise; rows fa2-skeleton.trace.cross, fa2-skeleton.trace.twin, fa2-skeleton.trace.oracle-f64)
    - noise floor (benchmarks/results/noise-floor.json) > fa2-speed-finalize / prev-karate: every committed output agrees (elementwise; rows fa2-skeleton.trace.cross, fa2-skeleton.trace.twin, fa2-skeleton.trace.oracle-f64)
    - noise floor (benchmarks/results/noise-floor.json) > records the rows, the adapters and the derived tolerances (GRAPHTY_NOISE_FLOOR_WRITE=1 only)
    - noise floor (benchmarks/results/noise-floor.json) > every tolerance is derived from a recorded basis row: floor <= value <= 10 x floor, factor = value / floor, no seed entry left

### P2-D1 -- deliverable: the WGSL compile matrix on both runtimes (every pipeline key of the node suite is covered by OVERRIDE_MATRIX: the test/setup/global.ts teardown throws otherwise, so a green node run is the proof)

- `test/kernel/wgsl-compile.test.ts` -- lavapipe: passed 13/13 (1.1 s); nvidia: passed 13/13 (4.1 s)
    - OVERRIDE_MATRIX (pure) > is bounded: the case count is the sum of the pins of the phases present in KERNELS
    - OVERRIDE_MATRIX (pure) > pins the per-kernel counts of the P1 and P2 entries
    - OVERRIDE_MATRIX (pure) > every case names a registry entry, uses only its declared overrides plus the standard pair (graph kernels only), carries snippets iff the entry has slots, matches the twin axis, and is unique
    - OVERRIDE_MATRIX (pure) > contains every entry's defaults, all four snippets of segmented-reduce, and each declared override toggled alone
    - OVERRIDE_MATRIX (pure) > matrixCovers: canonicalises omitted defaults and the standard pair, covers both twins in one call, ignores ad hoc ids, reports foreign combinations and snippets
    - OVERRIDE_MATRIX (pure) > the teardown's pieces: readKeyLogs accepts a JSON array, JSON lines and plain lines; assertMatrixCoverage names the uncovered key
    - compile matrix on Dawn (real device and backend=null) > compiles every case of degree on the real device and on backend=null (both twins where the entry has one)
    - compile matrix on Dawn (real device and backend=null) > compiles every case of reduce on the real device and on backend=null (both twins where the entry has one)
    - compile matrix on Dawn (real device and backend=null) > compiles every case of fill on the real device and on backend=null (both twins where the entry has one)
    - compile matrix on Dawn (real device and backend=null) > compiles every case of segmented-reduce on the real device and on backend=null (both twins where the entry has one)
    - compile matrix on Dawn (real device and backend=null) > compiles every case of fa2-repulsion-exact on the real device and on backend=null (both twins where the entry has one)
    - compile matrix on Dawn (real device and backend=null) > compiles every case of fa2-speed-finalize on the real device and on backend=null (both twins where the entry has one)
    - compile matrix on Dawn (real device and backend=null) > the keys these compiles created are all covered by the matrix (self-consistency of the coverage rule)
- `test/browser/compile-matrix.test.ts` -- swiftshader: passed 9/9 (9.4 s); chromium-nvidia: passed 9/9 (0.5 s)
    - compile matrix on Chromium > the feature context has subgroups (both CI browser adapters expose it) and the twin does not
    - compile matrix on Chromium > compiles every case of degree on Chromium (twin cases on both feature settings)
    - compile matrix on Chromium > compiles every case of reduce on Chromium (twin cases on both feature settings)
    - compile matrix on Chromium > compiles every case of fill on Chromium (twin cases on both feature settings)
    - compile matrix on Chromium > compiles every case of segmented-reduce on Chromium (twin cases on both feature settings)
    - compile matrix on Chromium > compiles every case of fa2-repulsion-exact on Chromium (twin cases on both feature settings)
    - compile matrix on Chromium > compiles every case of fa2-speed-finalize on Chromium (twin cases on both feature settings)
    - compile matrix on Chromium > compiled the whole matrix once (bounded: pipelines.size equals the case count)
    - compile matrix on Chromium > segmented-reduce thread-per-row on random1k (weighted sum): oracle within the analytic bound, twice bitwise, this adapter's noise fixture and oracle row written

### P2-D2 -- deliverable: the bind-group budget (<= 8 storage entries per stage; the 3.10.1 counts exactly)

- `test/kernel/bind-group-budget.test.ts` -- lavapipe: passed 9/9 (0.0 s); nvidia: passed 9/9 (0.0 s)
    - bind-group budget > every registered id has a row in the 3.10.1 table
    - bind-group budget > degree: <= 8 storage buffers per stage and exactly the 3.10.1 count
    - bind-group budget > reduce: <= 8 storage buffers per stage and exactly the 3.10.1 count
    - bind-group budget > fill: <= 8 storage buffers per stage and exactly the 3.10.1 count
    - bind-group budget > segmented-reduce: <= 8 storage buffers per stage and exactly the 3.10.1 count
    - bind-group budget > fa2-repulsion-exact: <= 8 storage buffers per stage and exactly the 3.10.1 count
    - bind-group budget > fa2-speed-finalize: <= 8 storage buffers per stage and exactly the 3.10.1 count
    - CAPS_INTEL_XE (spec 6: min != max) > the dispatch and upload planners give the spec-default answer under the 8 / 32 table
    - CAPS_INTEL_XE (spec 6: min != max) > the composer passes the pair through as the two pipeline constants and nothing else changes

### P2-D3 -- deliverable: UniformRing, Profiler, pipeline warm-up, residentBytes and its warning

- `test/kernel/uniform-ring.test.ts` -- lavapipe: passed 3/3 (0.0 s); nvidia: passed 3/3 (6.7 s)
    - UniformRing (spec 5.3) > offsetOf(k) === 256 k; reserve wraps; count > slots -> E_INVALID_ARGUMENT
    - UniformRing (spec 5.3) > binding() is the whole-buffer binding sized by the block; a block wider than a slot is E_INVALID_ARGUMENT
    - UniformRing (spec 5.3) > write + flush + a kernel reading slot k sees the right values through the dynamic offset; flush sends one writeBuffer of the dirty range
- `test/kernel/profiler.test.ts` -- lavapipe: passed 3/3 (0.1 s); nvidia: passed 3/3 (7.6 s)
    - Profiler (spec 5.5) > with timestamp-query granted: a pass timing per pass label, ns > 0, quantised false under Dawn
    - Profiler (spec 5.5) > a full query set drops timings; destroy is idempotent
    - Profiler (spec 5.5) > without the feature: ctx.profiler is null, and a hand-made Profiler is a no-op
- `test/memory/residency.test.ts` -- lavapipe: passed 22/22 (0.6 s); nvidia: passed 22/22 (17.2 s)
    - GraphResidency: the upload contract (spec 11.3, gpu-upload.test.ts lines 254-666) > the device accepts the format's 256-byte segment alignment and default-limit planning assumptions
    - GraphResidency: the upload contract (spec 11.3, gpu-upload.test.ts lines 254-666) > arena path: ONE buffer of the hot prefix; per-segment bindings at segment.byteOffset - arena.byteOffset equal the CPU views
    - GraphResidency: the upload contract (spec 11.3, gpu-upload.test.ts lines 254-666) > cold segments: named in the first core() they ride the full arena; needed later they get their own buffer
    - GraphResidency: the upload contract (spec 11.3, gpu-upload.test.ts lines 254-666) > perArray path: fromCsr (arena === null) and transpose() upload one buffer per array with whole-buffer bindings
    - GraphResidency: the upload contract (spec 11.3, gpu-upload.test.ts lines 254-666) > undirected: reverse() aliases the forward arrays, inDegree() is outDegree(), both degree orders are one view
    - GraphResidency: the upload contract (spec 11.3, gpu-upload.test.ts lines 254-666) > identity permutations are never materialised: the arcToEdge / edgeToArc getters are not read, byteLength({ views: true }) is unchanged
    - GraphResidency: the upload contract (spec 11.3, gpu-upload.test.ts lines 254-666) > packed columns through column(): u8 -> padded u32 words, bool -> bitmap words, f64 -> the cached f32 copy, u8 x3
    - GraphResidency: the upload contract (spec 11.3, gpu-upload.test.ts lines 254-666) > the same array object uploads once: a view twice, an ad hoc array twice, a u8 column twice
    - GraphResidency: the upload contract (spec 11.3, gpu-upload.test.ts lines 254-666) > a column version bump re-uploads in place (same buffer, new version); an unchanged column is never re-uploaded
    - GraphResidency: the upload contract (spec 11.3, gpu-upload.test.ts lines 254-666) > ctx.release destroys every buffer of the snapshot, leaves allocator.liveBuffers at 0 and trims the pool
    - GraphResidency: the upload contract (spec 11.3, gpu-upload.test.ts lines 254-666) > E_RELEASED: a live user sees isReleased; view / column / array on the released snapshot throw; core() re-uploads
    - GraphResidency: the upload contract (spec 11.3, gpu-upload.test.ts lines 254-666) > release is idempotent and safe on a snapshot never uploaded
    - GraphResidency: the upload contract (spec 11.3, gpu-upload.test.ts lines 254-666) > withColumns() siblings share one record: one core buffer, one stats entry, a release of either tombstones both
    - GraphResidency: the upload contract (spec 11.3, gpu-upload.test.ts lines 254-666) > warns once through options.warn when more than warnUnreleasedSnapshots snapshots are resident
    - GraphResidency: the upload contract (spec 11.3, gpu-upload.test.ts lines 254-666) > a container adopted by fromBytes uploads straight from the file bytes (arena.byteOffset !== 0), also through a view at byte 8
    - GraphResidency: the upload contract (spec 11.3, gpu-upload.test.ts lines 254-666) > array(): keyed on the object, registered against an owner or destroyed by the caller; invalid inputs rejected
    - GraphResidency: the upload contract (spec 11.3, gpu-upload.test.ts lines 254-666) > view(): P7 names and packViews are E_UNSUPPORTED, an empty snapshot is E_INVALID_ARGUMENT; core() of arcCount 0 binds rowPtr only
    - GraphResidency: the upload contract (spec 11.3, gpu-upload.test.ts lines 254-666) > a windowed plan is E_TOO_LARGE { path: 'windowed' } until P4 executes windows (PLAN DECISION 2)
    - GraphResidency: the upload contract (spec 11.3, gpu-upload.test.ts lines 254-666) > a detached snapshot is E_SNAPSHOT { reason: 'detached' } from core / view / column / array
    - GraphResidency: the upload contract (spec 11.3, gpu-upload.test.ts lines 254-666) > clearOnLoss drops every record without destroying; destroyAll destroys everything and disposes
    - P2: residentBytes and stats().perSnapshot (spec 4.1) > residentBytes equals the sum of the uploaded byte lengths; perSnapshot itemises them; release zeroes them
    - P2: residentBytes and stats().perSnapshot (spec 4.1) > clearOnLoss() forgets every record without destroying and zeroes residentBytes
- `test/kernel/pipeline-cache.test.ts` -- lavapipe: passed 9/9 (0.1 s); nvidia: passed 9/9 (10.0 s)
    - pipelineKey / PipelineCache.key > is id | stableJson(overrides) | present needs | hash(snippets), independent of the override key order
    - pipelineKey / PipelineCache.key > lists a need only when the caps have the feature, and hashes the snippets
    - pipelineKey / PipelineCache.key > the cache's key() agrees with pipelineKey on the context's caps
    - PipelineCache > get twice -> one pipeline; concurrent gets share one compile; kernel() is cached with it
    - PipelineCache > a body with a WGSL error -> E_SHADER_COMPILE { id, stage: 'compile', messages } with body-relative lines
    - PipelineCache > a compose error rejects get() with stage 'compose' before any device work
    - PipelineCache > a pipeline error that is not a compilation message (an oversized WG) -> E_VALIDATION
    - PipelineCache > warm compiles every spec once and keys() lists them in creation order
    - PipelineCache > dispose() forgets the pipelines and refuses further compiles with E_DISPOSED

RESULT: every G2 file ran in every run it belongs to and every test passed.

## 5. The segmented-reduce noise-floor rows

`node tmp/g2/check-noise-rows.mjs` (contract 5.6; the fixtures are P2-T2's
`test/fixtures/noise/segmented-reduce-*.json`, one per adapter class,
written with `GRAPHTY_NOISE_FLOOR_WRITE=1` on each adapter; the rows are in
`benchmarks/results/noise-floor.json`):

Fixtures (test/fixtures/noise):

| File                                                        | Fixture  | Adapter class              | dtype | Values |
| ----------------------------------------------------------- | -------- | -------------------------- | ----- | ------ |
| `segmented-reduce-random1k-google-swiftshader-browser.json` | random1k | google-swiftshader-browser | f32   | 1000   |
| `segmented-reduce-random1k-mesa-software-node.json`         | random1k | mesa-software-node         | f32   | 1000   |
| `segmented-reduce-random1k-nvidia-lovelace-browser.json`    | random1k | nvidia-lovelace-browser    | f32   | 1000   |
| `segmented-reduce-random1k-nvidia-lovelace-node.json`       | random1k | nvidia-lovelace-node       | f32   | 1000   |

- fixture from NVIDIA Dawn (node): nvidia-lovelace-node
- fixture from lavapipe Dawn (node): mesa-software-node
- fixture from SwiftShader Chromium (browser): google-swiftshader-browser

Rows of benchmarks/results/noise-floor.json with kernel "segmented-reduce" (recordedAt 2026-09-15T15:44:36.579Z):

| id                                                                          | fixture  | comparison    | a                          | b                          | maxRelError           | maxAbsError             | samples |
| --------------------------------------------------------------------------- | -------- | ------------- | -------------------------- | -------------------------- | --------------------- | ----------------------- | ------- |
| segmented-reduce.sum.oracle-f64.google-swiftshader-browser                  | random1k | oracle-f64    | google-swiftshader-browser | oracle-f64                 | 1.7834327718913287e-7 | 0.000006079673767089844 | 1000    |
| segmented-reduce.sum.oracle-f64.nvidia-lovelace-browser                     | random1k | oracle-f64    | nvidia-lovelace-browser    | oracle-f64                 | 1.7834327718913287e-7 | 0.000006079673767089844 | 1000    |
| segmented-reduce.sum.oracle-f64.nvidia-lovelace-node                        | random1k | oracle-f64    | nvidia-lovelace-node       | oracle-f64                 | 1.7834327718913287e-7 | 0.000006079673767089844 | 1000    |
| segmented-reduce.sum.cross.nvidia-lovelace-node--google-swiftshader-browser | random1k | cross-adapter | nvidia-lovelace-node       | google-swiftshader-browser | 0                     | 0                       | 1000    |
| segmented-reduce.sum.cross.nvidia-lovelace-node--mesa-software-node         | random1k | cross-adapter | nvidia-lovelace-node       | mesa-software-node         | 0                     | 0                       | 1000    |
| segmented-reduce.sum.cross.nvidia-lovelace-node--nvidia-lovelace-browser    | random1k | cross-adapter | nvidia-lovelace-node       | nvidia-lovelace-browser    | 0                     | 0                       | 1000    |
| segmented-reduce.sum.oracle-f64.mesa-software-node                          | random1k | oracle-f64    | mesa-software-node         | oracle-f64                 | 1.7834327718913287e-7 | 0.000006079673767089844 | 1000    |
| segmented-reduce.sum.cross.mesa-software-node--google-swiftshader-browser   | random1k | cross-adapter | mesa-software-node         | google-swiftshader-browser | 0                     | 0                       | 1000    |
| segmented-reduce.sum.cross.mesa-software-node--nvidia-lovelace-browser      | random1k | cross-adapter | mesa-software-node         | nvidia-lovelace-browser    | 0                     | 0                       | 1000    |
| segmented-reduce.sum.cross.mesa-software-node--nvidia-lovelace-node         | random1k | cross-adapter | mesa-software-node         | nvidia-lovelace-node       | 0                     | 0                       | 1000    |

- cross-adapter row covering NVIDIA Dawn (node): nvidia-lovelace-node
- cross-adapter row covering lavapipe Dawn (node): mesa-software-node
- cross-adapter row covering SwiftShader Chromium (browser): google-swiftshader-browser
- twin rows: 0

Tolerances whose basis is one of these rows:

- segmented-reduce.cross: value 0.000001, basis segmented-reduce.sum.oracle-f64.nvidia-lovelace-node (maxRelError 1.7834327718913287e-7), factor 5.607163980392157 -> valid (<= 10x the basis)

RESULT: the three adapters are present in the fixtures and the cross-adapter rows.

Reading: every cross-adapter pair of the four classes is bitwise equal
(maxRelError 0 -- the thread-per-row tier folds each row's arcs serially
in arc order, so every adapter sums in the same order), the f32 error
against the f64 oracle is 1.783e-7 on all four, and the one tolerance
based on these rows (`segmented-reduce.cross` = 1e-6, basis
`segmented-reduce.sum.oracle-f64.nvidia-lovelace-node`, factor 5.6) is
within the 10x rule. No twin row is recorded for this kernel: the twin
agreement ("TIER 0 calls no reduction helper", bitwise) is asserted
in-process by `test/primitives/segmented-reduce.test.ts` on every run of
section 4, including the `lavapipe/no-subgroups` pass. The fourth
fixture (`nvidia-lovelace-browser`, from the NVIDIA Chromium run) is
beyond the three adapters the checklist asks for.

## 6. Decisions and deviations

### Decision G2-D1 -- the "UniformBlock negative test rejected on Chromium" leg cannot be shown on the lanes' Chromium (owner decision pending)

- Measured (2026-09-15, `tmp/g2/uniform-layout-probe.mjs`, output in
  `tmp/g2/uniform-layout-probe.txt`; reproduces P2-T1's finding in the
  header of `test/browser/uniform-layout.test.ts`): the WGSL 14.4.5
  "Invalid" struct (`struct BadInner { x: f32 }` followed by `b: f32` at
  offset 4 in a `var<uniform>`) is ACCEPTED by the Chromium the lanes and
  the dev box run -- Playwright 1.63.0's Chrome for Testing 153.0.8010.12
  (headless shell 1243), which lists `uniform_buffer_standard_layout`
  among 13 `wgslLanguageFeatures` -- with and without
  `--disable-dawn-features=allow_unsafe_apis`; Chromium 145 (headless
  shell 1208) lists the feature and accepts it too; Chromium 143
  (headless shell 1200) does not list the feature and accepts the struct
  under `--enable-unsafe-webgpu` (Dawn's `allow_unsafe_apis` relaxes
  Tint's check), rejecting it only under
  `--disable-dawn-features=allow_unsafe_apis`, at `createShaderModule`
  (a Tint compilation error in `getCompilationInfo()`: "'uniform' storage
  requires that the number of bytes between the start of the previous
  member of type struct and the current member be a multiple of 16 bytes,
  but there are currently 4 bytes between 'a' and 'b'. Consider setting
  '@align(16)' on this member"). Dawn-node (webgpu@0.4.0) lists the
  feature and accepts the struct on lavapipe and on the RTX 4070 SUPER
  (`test/kernel/uniform-layout.test.ts` prints
  `[uniform-layout] runtime=node uniform_buffer_standard_layout=true hand-written struct accepted=true`).
  The generated `UniformBlock` text compiles everywhere.
- What the landed tests prove instead: `test/browser/uniform-layout.test.ts`
  and `test/kernel/uniform-layout.test.ts` assert the invariant
  `accepted === wgslLanguageFeatures.has("uniform_buffer_standard_layout")`
  (with a non-empty message on rejection) and that the generated struct
  compiles on both runtimes -- green on SwiftShader, NVIDIA Chromium,
  lavapipe and the RTX 4070 SUPER (section 4, G2-4). The checklist text
  "rejected on Chromium" (contract 7.3; spec 5.3 "rejected on Chromium and
  accepted on Dawn-node -- the reason the generator exists") pinned
  Chromium 139 (note 05 section 3.2), which no longer exists in the
  toolchain the spec's 2.5 versions resolve to (`playwright ^1.54.1` ->
  1.63.0 -> Chromium 153).
- Options considered: (a) accept the invariant form as G2-4's proof and
  keep the probe's Chromium-143 rejection (recorded above) as the evidence
  that a strict-layout runtime rejects the hand-written struct -- no file
  changes; the generator's reason stands (a runtime without the feature,
  or a user agent that lands `allow_unsafe_apis` off, rejects the struct);
  (b) add `--disable-dawn-features=allow_unsafe_apis` to `BROWSER_FLAGS`
  in `vitest.config.ts` (P0-T2's file) AND pin Playwright to a Chromium <=
  143 (a release bundling headless shell 1200 or older) against spec 2.5's `^1.54.1` -- shows the rejection on the
  lanes at the cost of a downgrade and of `--enable-unsafe-webgpu`'s
  other effects, and Chromium 145+ would accept the struct again; (c)
  re-word the checklist item and spec 5.3 to the invariant (an owner edit
  of the contract and the spec's Review log per contract section 0).
- Decision (owner, 2026-09-__): _______ (P2-T3 proposes (a) + (c): no
  test, config or version changes; the record above is the evidence).
- Re-measured: not applicable unless (b) is chosen.

### Deviations of P2-T3 from its plan part (no owner decision needed)

- Step 2 / Step 13 `check`: the script's precondition (P1 committed) does
  not hold yet, so `bash tmp/commit-p2.sh check` reports 10 `ERROR:` lines
  (`src/context.ts`, `src/kernel/profiler.ts`, `src/memory/buffer-pool.ts`,
  `src/memory/residency.ts`, `src/kernels.ts`, `test/device/lost.test.ts`,
  `test/memory/residency.test.ts`, `test/helpers/caps-tables.ts`,
  `test/helpers/sabotage.ts`, `benchmarks/results/noise-floor.json` "is not
  tracked (expected an edit of a P0 / P1 file)") and lists every
  uncommitted P0-T4 / housekeeping / P1 path as
  `WARNING: changes outside the P2 lists`. Both clear once the owner runs
  `tmp/commit-p0-record.sh`, `tmp/commit-housekeeping.sh` and `tmp/commit-p1.sh` (in that order, then
  `tmp/commit-p2.sh code`). Note for the owner: `commit-p1.sh` stages the
  working-tree state of those P1 files, which already carries P2-T1's and
  P2-T2's edits, so the P1 commits will contain them and
  `commit-p2.sh code` will print `WARNING: ... shows no change` for the ten edited files
  (a warning, not an error; the P2 commit then holds the 24 new files, the
  `test/setup/global.ts` edit and the four fixtures).
- Step 3: `lane-durations.mjs` ran against the live API (the P0-T3 runs,
  section 1) and behaved as specified (exit 1 for a failed run; exit 1
  with "no completed run" for gpu.yml); OVERHEAD = 5 is spec 12.6's
  number, as the plan's fallback prescribes.
- Step 5: the node leg of `tmp/g2/evidence-nvidia.sh` carries
  `LD_LIBRARY_PATH=<egl tree>` (the plan's script did not): without it
  Dawn-in-Node lists only llvmpipe on this box and
  `GRAPHTY_GPU_REQUIRE=hardware` fails with `E_SOFTWARE_ONLY` (package
  CLAUDE.md, verified spelling). Its "adapters seen" grep also matches the
  `vendor=nvidia ... device=nvidia-geforce-rtx-4070-super` line the setup
  prints (the description string is `NVIDIA: 580.173.02 580.173.2.0`, not
  "NVIDIA GeForce ...").
- Step 8: exercised on the dev-box report (section 3) because no
  GPU-lane artifact exists.
- Step 10: the CLAUDE.md rows carry the MEASURED facts, not the plan's
  pre-written text: the misaligned struct is accepted by Chromium 153
  (G2-D1), and the browser's delivery of a bad bind group's error reached
  the batch's own readback (through `assertReady()` after `mapAsync`
  resolved) on both Chromium adapters, not the plan's "thrown from the
  next assertReady()" fallback
  (`[browser/batch] bad bind group on swiftshader|nvidia delivered through readback` in
  `tmp/g2/browser-swiftshader.log` / `browser-nvidia.log`). The delivery
  ORDER itself was measured by two probes of this task (section 7).
- knip prints two configuration hints (`test/fixtures/**/*.ts` and
  `benchmarks/layout-run.ts`: "Refine entry pattern (no matches)") and
  exits 0; both patterns are P0-T2's `knip.config.ts` entries for files
  later phases create, present since P1's green check
  (`tmp/p1t7-fix-knip.log`).
- The T-1 1M/10M miss recorded in G1.md section 7 is an open owner item
  P2 does not act on.
- Step 1 (`tmp/commit-p2.sh`, after the spec-compliance review): the
  plan's two commit messages said "the misaligned-uniform negative test
  (rejected by Chromium, accepted by Dawn-node)" and "rejected by
  Chromium 139", which decision G2-D1 above contradicts (accepted by
  Chromium 153, feature listed; rejected only on Chromium 143 with
  `allow_unsafe_apis` off). Both messages now state the measured fact:
  the test asserts `accepted === has(uniform_buffer_standard_layout)`
  on both runtimes and the Chromium rejection leg is decision G2-D1.
  The `record` mode also refuses to commit while the decision line of
  G2-D1 is still blank (its placeholder day and the underscore run are
  grepped for, in addition to the double-brace field check), so the
  owner fills the decision before running it.
- `CLAUDE.md`: besides P1-T7's Benchmarks section and fact rows and
  this task's "Settled at G2" block, the uncommitted diff carries one
  sentence rewrite in the Layers section (`test/build-output.test.ts`
  "(from P1-T7) asserts" -> "asserts"; 2 deletions, 1 insertion) that
  neither P1-T7's Step 38 nor this task's Step 10 prescribes and no
  scratch record attributes. It predates this task
  (`tmp/g2/CLAUDE.md.before-prettier` already carries it), is true now
  that the file exists, and rides into the `CLAUDE.md` stage of
  `tmp/commit-p1.sh`; the owner confirms it there.

## 7. Facts recorded in CLAUDE.md at G2 ("Verified Platform Facts" -> "Settled at G2")

- `uncapturederror` delivery order. Measured by `tmp/g2/uncaptured-order.mjs`
  (Dawn-node) and `tmp/g2/uncaptured-order-browser.mjs` (Chromium) on
  2026-09-15, outputs in `tmp/g2/uncaptured-order-*.txt`, for the two P2
  cases (a STORAGE-only buffer in a UNIFORM slot of `createBindGroup`; a
  command buffer whose copy is out of range, invalid at `finish()` and
  again at `submit()`): Dawn-node (webgpu@0.4.0) delivers SYNCHRONOUSLY on
  lavapipe and on the RTX 4070 SUPER alike -- the listener has run before
  `createBindGroup()` / `finish()` / `queue.submit()` return (event counts
  0 -> 1 across the call) -- which is why `CommandBatch.submit()` reads the
  context's pending-error slot right after `submit()` and rejects ITS OWN
  readback with `E_VALIDATION { batchId, batchLabel }`
  (`test/kernel/batch.test.ts` prints
  `[batch] bad bind group -> E_VALIDATION batchId=1 ...`). Chromium 153 (SwiftShader and NVIDIA Vulkan alike)
  delivers ASYNCHRONOUSLY: the count is still 0 when the failing call
  returns and the event has arrived by the time `queue.onSubmittedWorkDone()`
  resolves (the second event of the invalid-command-buffer case one
  macrotask later); in the P2 batch that error is therefore found by the
  `assertReady()` that follows the staging slot's `mapAsync` and the same
  batch's readback still rejects with the batch id
  (`test/browser/batch.test.ts`, `delivered through readback` on both
  adapters). The Tesla T4 lane has not run (section 1).
- The misaligned hand-written uniform struct (spec 5.3, R-10, D20):
  decision G2-D1 above carries the measurements; the CLAUDE.md row states
  them per runtime (Dawn-node: accepted, feature listed; Chromium 153:
  accepted, feature listed; the rejection exists on Chromium 143 only with
  `--disable-dawn-features=allow_unsafe_apis`, at `createShaderModule`),
  settled by `test/kernel/uniform-layout.test.ts`,
  `test/browser/uniform-layout.test.ts` and `tmp/g2/uniform-layout-probe.mjs`.

## 8. How to reproduce

From `packages/webgpu-graph-algorithms` on the dev box (scripts under the
gitignored `tmp/g2/` are reproduced from the P2 plan,
`docs/superpowers/plans/2026-09-15-webgpu-p2.md`, task P2-T3; the two
delivery-order probes and the uniform-layout probe are P2-T3's additions,
described in section 7 and section 6):

```
bash tmp/g2/time-default-lane.sh <overhead minutes>   # the default lane locally on lavapipe (timed, JSON reporter kept)
bash tmp/g2/evidence-nvidia.sh                         # node on the RTX 4070 SUPER, browser on NVIDIA Chromium
node tmp/g2/g2-map.mjs > tmp/g2/g2-map.md              # section 4 (and the run summaries of section 2)
node tmp/g2/check-noise-rows.mjs                       # section 5
node tmp/g2/lane-durations.mjs ci.yml master <sha>     # section 2, default lane
node tmp/g2/lane-durations.mjs gpu.yml master <sha>    # section 2, GPU lane
gh run download <gpu run id> -n gpu-results-<gpu run id> -D tmp/g2/gpu-results-<gpu run id>
node tmp/g2/check-gpu-report.mjs tmp/g2/gpu-results-<gpu run id>   # section 3
node tmp/g2/uncaptured-order.mjs adapter=llvmpipe      # section 7, Dawn-node on lavapipe (VK_DRIVER_FILES set); without the flag: NVIDIA
node tmp/g2/uncaptured-order-browser.mjs swiftshader   # section 7, Chromium (or: nvidia)
node tmp/g2/uniform-layout-probe.mjs <label> [--exe=<chromium>] [--disable-dawn-features=allow_unsafe_apis]   # G2-D1
```

The default-lane environment is spec 12.2's: `GRAPHTY_GPU_ADAPTER=llvmpipe
GRAPHTY_GPU_REQUIRE=any VK_DRIVER_FILES=/usr/share/vulkan/icd.d/lvp_icd.x86_64.json
XDG_RUNTIME_DIR=/tmp` for the node steps and `GRAPHTY_BROWSER_GPU=swiftshader
GRAPHTY_GPU_REQUIRE=any` for the browser step; the NVIDIA legs use
`GRAPHTY_GPU_REQUIRE=hardware` with `GRAPHTY_GPU_ADAPTER` unset and
`LD_LIBRARY_PATH=/home/apowers/Projects/webgpu-graph-algorithms/tmp/egl/root/usr/lib/x86_64-linux-gnu`
(Dawn-in-Node lists the NVIDIA adapter only with it) and, for Chromium,
`GRAPHTY_BROWSER_GPU=nvidia` with `libEGL.so.1` reachable through
`GRAPHTY_EGL_LIB_DIR` / `LD_LIBRARY_PATH` (`docs/HEADLESS_GPU_REPORT.md`).
