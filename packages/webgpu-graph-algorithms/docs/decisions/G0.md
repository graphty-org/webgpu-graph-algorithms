OPEN RECORD (2026-09-16): the org / runner / image rows below are unfilled because the hosted `gpu-linux-t4` runner was never provisioned (design 12.4 is an owner item). Committed as-is so the record travels with the package into graphty-monorepo; Phase M4 of `design/webgpu/plans/2026-09-16-graphty-monorepo-integration.md` fills and closes it there.

# G0 -- gate record for P0 (reset + package skeleton)

Spec: `design/webgpu-acceleration-plan.md` section 13 row P0 (gate G0), 12.2, 12.4; D21.
Contract: `docs/superpowers/plans/2026-09-14-webgpu-p0-p3-interfaces.md` section 7.1 row P0-T4 and
its owner-items paragraph; section 9 items 1, 2, 4, 7, 8.
Recorded by: P0-T4 (local legs, templates, the record) and the owner (org / repository settings, the
GitHub runs, the sign-off). Date closed: [[fill: YYYY-MM-DD of the sign-off in section 11]].

Rule (spec 10.4): a number in this file is a MEASURED one, with the command or run that produced it;
a target that was missed is re-fixed by a recorded owner decision, never relaxed silently. Nothing in
this file is copied from memory or from the spec's expectations.

## 1. Gate items (spec 13 row P0, contract 7.1)

| #     | Gate item                                                                                                         | Status                                                                                                                                                                                                                                                                                            | Evidence (section)    |
| ----- | ----------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------- |
| G0-1  | The default lane ran on GitHub                                                                                    | [[fill: PASS / FAIL -- owner step 11 / 12]]                                                                                                                                                                                                                                                       | 4 (D1, D2)            |
| G0-2  | `pnpm run build` (= `build:all`), `lint` (incl. the strict-consumer compile), `knip` pass                         | [[fill: PASS / FAIL -- B1 green locally (0 / 0 / 0); awaits the CI run D1 (step 11)]]                                                                                                                                                                                                             | 3 (B1), 4 (D1)        |
| G0-3  | The `node` project acquires an adapter and prints `adapter.info` on the dev box (NVIDIA)                          | PASS                                                                                                                                                                                                                                                                                              | 3 (L1)                |
| G0-4  | ... on the dev box with `GRAPHTY_GPU_ADAPTER=llvmpipe`                                                            | PASS                                                                                                                                                                                                                                                                                              | 3 (L2)                |
| G0-5  | ... on `ubuntu-latest` (lavapipe)                                                                                 | [[fill: PASS / FAIL -- owner step 11]]                                                                                                                                                                                                                                                            | 4 (D4)                |
| G0-6  | The `browser` project acquires SwiftShader on `ubuntu-latest`                                                     | [[fill: PASS / FAIL -- owner step 11]]                                                                                                                                                                                                                                                            | 4 (D5)                |
| G0-7  | The `browser` project acquires NVIDIA locally with the four flags                                                 | PASS                                                                                                                                                                                                                                                                                              | 3 (L3)                |
| G0-8  | The deliberate red run (`GRAPHTY_GPU_REQUIRE=nvidia` on the software lane fails) recorded in the PR               | [[fill: PASS / FAIL -- owner step 15]]                                                                                                                                                                                                                                                            | 6 (R1-R6), 8 (S7)     |
| G0-9  | `gpu-report.js` exits non-zero on a software adapter under `nvidia` and the step is red (no `tee`)                | [[fill: PASS / FAIL -- P6 exits 2 locally with the policy reason; awaits the red run R2-R4, R6 (step 15)]]                                                                                                                                                                                        | 3 (P6), 6 (R2-R4, R6) |
| G0-10 | The 7.2 sign-off recorded (D21), with the four contract corrections accepted or re-decided                        | [[fill: PASS / FAIL -- owner step 12 / 17]]                                                                                                                                                                                                                                                       | 8 (S1-S6)             |
| G0-11 | The Vitest 3 per-instance `launch` spelling recorded in `CLAUDE.md`                                               | [[fill: PASS -- after the step-16 splice: the launch-spelling row is present in the P0-T3 section today (CLAUDE.md line 264 and Testing) and V1 is filled against the installed 3.2.7 files; re-confirm the row in the NEW Verified Platform Facts section once tmp/g0/splice-facts.mjs has run]] | 7 (V1)                |
| G0-12 | Public repository; `repository.url` confirmed; the `gpu` label                                                    | [[fill: PASS / FAIL -- owner steps 7, 10]]                                                                                                                                                                                                                                                        | 2 (O1, O2, O9)        |
| G0-13 | GitHub Team plan; runner `gpu-linux-t4` in group `gpu` limited to this repository; $50 limit + 75% alert          | [[fill: PASS / FAIL -- owner steps 8, 9]]                                                                                                                                                                                                                                                         | 2 (O3-O6)             |
| G0-14 | Read-only default workflow permissions; approval required for all external contributors                           | [[fill: PASS / FAIL -- owner step 10]]                                                                                                                                                                                                                                                            | 2 (O7, O8)            |
| G0-15 | First `workflow_dispatch` of `gpu.yml`: the image facts recorded (Ubuntu, glibc, driver, modprobe, Xvfb, libegl1) | [[fill: PASS / FAIL -- owner steps 13, 14]]                                                                                                                                                                                                                                                       | 5 (F1-F20)            |
| G0-16 | GPU lane registered and green on `master`                                                                         | [[fill: PASS / FAIL -- owner step 13]]                                                                                                                                                                                                                                                            | 5 (M1)                |
| G0-17 | `test.env` versus `define` in browser mode measured (contract 9 item 4)                                           | PASS                                                                                                                                                                                                                                                                                              | 7 (E1, E2)            |
| G0-18 | `benchmarks/results/gpu-linux-t4.json`: no baseline yet (deferred to P1-T7)                                       | recorded                                                                                                                                                                                                                                                                                          | 9                     |

## 2. Organisation and repository provisioning (spec 12.4; owner)

| Id  | Item                                                                                                                                                      | Command / UI path                                                                                                                                   | Recorded result                                                                                             |
| --- | --------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------- |
| O1  | Repository public                                                                                                                                         | `gh repo edit graphty-org/webgpu-graph-algorithms --visibility public`; verify `gh api /repos/graphty-org/webgpu-graph-algorithms --jq .visibility` | [[fill: the printed visibility and the date]]                                                               |
| O2  | `repository.url` / `directory`                                                                                                                            | `grep -A3 '"repository"' package.json`                                                                                                              | [[fill: the three lines verbatim]]                                                                          |
| O3  | GitHub Team plan (larger runners are gated on it)                                                                                                         | UI: Organization > Settings > Billing and plans > Plans > Upgrade to Team; verify `gh api /orgs/graphty-org --jq .plan.name`                        | [[fill: plan name printed, date, seats]]                                                                    |
| O4  | Runner group `gpu`, repository access limited to this repository, public repositories allowed                                                             | UI or `gh api` (step 8) ; verify `gh api /orgs/graphty-org/actions/runner-groups`                                                                   | [[fill: group id, visibility, allows_public_repositories, the repository list]]                             |
| O5  | Hosted runner `gpu-linux-t4`: Linux x64, the 4-core GPU size (1x Tesla T4 16 GB, 28 GB RAM), the NVIDIA partner image, group `gpu`, maximum concurrency 1 | UI or `gh api` (step 8); verify `gh api /orgs/graphty-org/actions/hosted-runners`                                                                   | [[fill: runner id, name, status, image display name and source, size id, maximum_runners, runner_group_id]] |
| O6  | Actions spending limit $50 per month with the 75% e-mail alert                                                                                            | UI only (step 9)                                                                                                                                    | [[fill: budget name / type, amount, "stop usage at limit" on or off, alert thresholds, recipients, date]]   |
| O7  | Default workflow permissions read-only, no PR approval by Actions                                                                                         | `gh api -X PUT .../actions/permissions/workflow` (step 10); verify GET                                                                              | [[fill: the GET response verbatim]]                                                                         |
| O8  | Approval required for all external contributors                                                                                                           | `gh api -X PUT .../actions/permissions/fork-pr-contributor-approval` (step 10); verify GET                                                          | [[fill: the GET response verbatim]]                                                                         |
| O9  | The `gpu` label                                                                                                                                           | `gh api -X POST .../labels` (step 10); verify `gh api .../labels --jq '.[].name'`                                                                   | [[fill: the label list line containing gpu; colour; description]]                                           |
| O10 | Token scopes for the org-level API (`admin:org`)                                                                                                          | `gh auth refresh -h github.com -s admin:org`                                                                                                        | [[fill: done / not needed (UI used)]]                                                                       |

## 3. Local legs on the dev box (P0-T4; `tmp/g0/local-runs.sh`)

Dev box: Docker container, Ubuntu 22.04.5, glibc 2.35, NVIDIA driver 580.173.02, RTX 4070 SUPER;
headless Chromium reaches the GPU only with `libEGL.so.1` on the child's `LD_LIBRARY_PATH`
(`GRAPHTY_EGL_LIB_DIR`) plus the four Vulkan flags (`docs/HEADLESS_GPU_REPORT.md`).

| Id  | Leg                                                                                 | Command (environment)                                                                                                                                                                                                                                            | Exit      | Recorded lines                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| --- | ----------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| B1  | build / lint / knip                                                                 | `pnpm run build:all && pnpm run lint && (cd .. && pnpm exec knip)`                                                                                                                                                                                               | 0 / 0 / 0 | build: `Successfully created dist/{webgpu-graph-algorithms,browser,node}.d.ts` (stderr: `Generated an empty chunk: "browser"`, the empty P0 browser entry); lint: `> eslint && tsc --noEmit -p tsconfig.json && tsc -p tsconfig.strict-consumer.json` with no diagnostic after it (eslint, tsc and the strict-consumer compile against `dist/` all silent); knip: `Configuration hints (2)` (stderr: `test/fixtures/**/*.ts ... Refine entry pattern (no matches)`, `benchmarks/layout-run.ts ... Refine entry pattern (no matches)` -- hints for P1+ globs, not findings; exit 0). Measured 2026-09-15 (`tmp/g0/{build,lint,knip}.out`)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| L1  | `node` project on NVIDIA                                                            | `LD_LIBRARY_PATH=/home/apowers/Projects/webgpu-graph-algorithms/tmp/egl/root/usr/lib/x86_64-linux-gnu GRAPHTY_GPU_REQUIRE=hardware pnpm exec vitest run --project=node` (the libEGL tree is REQUIRED for Dawn-in-Node on this box, see the note under the table) | 0         | `[gpu] adapter vendor=nvidia architecture=lovelace device=nvidia-geforce-rtx-4070-super description=NVIDIA: 580.173.02 580.173.2.0 software=false subgroups=32..32 features=bgra8unorm-storage,clip-distances,core-features-and-limits,depth-clip-control,depth32float-stencil8,dual-source-blending,float32-blendable,float32-filterable,indirect-first-instance,primitive-index,rg11b10ufloat-renderable,subgroups,texture-component-swizzle,texture-compression-bc,texture-compression-bc-sliced-3d,texture-formats-tier1,texture-formats-tier2,timestamp-query`; `[gpu] adapter limits maxBufferSize=1099511627776 maxStorageBufferBindingSize=2147483644 maxStorageBuffersPerShaderStage=16 maxComputeWorkgroupsPerDimension=65535`; `[gpu] policy GRAPHTY_GPU_REQUIRE=hardware level=hardware vendor=- ok=true`; `Test Files 7 passed (7)`, `Tests 96 passed / 1 skipped (97)` (the skip is `test/build-output.test.ts` "the tsc output and the bundle exist under CI", a CI-only assertion, not a GPU skip), `Duration 1.52s`. Measured 2026-09-15 (`tmp/g0/node-nvidia.{out,err}`)                                                                                                                                                                                                                                                      |
| L2  | `node` project on lavapipe                                                          | `GRAPHTY_GPU_ADAPTER=llvmpipe GRAPHTY_GPU_REQUIRE=any VK_DRIVER_FILES=/usr/share/vulkan/icd.d/lvp_icd.x86_64.json XDG_RUNTIME_DIR=/tmp pnpm exec vitest run --project=node`                                                                                      | 0         | `[gpu] adapter vendor=mesa architecture=software device=llvmpipe-llvm-15-0-7-256-bits- description=llvmpipe: Mesa 23.2.1-1ubuntu3.1~22.04.3 (LLVM 15.0.7) 0.0.1 software=true subgroups=8..8 features=bgra8unorm-storage,clip-distances,core-features-and-limits,depth-clip-control,depth32float-stencil8,dual-source-blending,float32-blendable,float32-filterable,indirect-first-instance,primitive-index,rg11b10ufloat-renderable,shader-f16,subgroups,texture-component-swizzle,texture-compression-bc,texture-compression-bc-sliced-3d,timestamp-query`; `[gpu] adapter limits maxBufferSize=4294967295 maxStorageBufferBindingSize=134217728 maxStorageBuffersPerShaderStage=16 maxComputeWorkgroupsPerDimension=65535`; `[gpu] policy GRAPHTY_GPU_REQUIRE=any level=any vendor=- ok=true`; `Test Files 7 passed (7)`, `Tests 96 passed / 1 skipped (97)` (the same CI-only skip as L1), `Duration 1.06s`. Measured 2026-09-15 (`tmp/g0/node-llvmpipe.{out,err}`)                                                                                                                                                                                                                                                                                                                                                                         |
| L3  | `browser` project on NVIDIA, four flags                                             | `GRAPHTY_BROWSER_GPU=nvidia GRAPHTY_GPU_REQUIRE=nvidia GRAPHTY_EGL_LIB_DIR=/home/apowers/Projects/webgpu-graph-algorithms/tmp/egl/root/usr/lib/x86_64-linux-gnu node scripts/run-browser-project.js`                                                             | 0         | `numTotalTests=4 numPassedTests=4 numFailedTests=0`; `vitest exited 0` (no exit 124: the `browser.close()` hang did not occur); the test's adapter line: `[browser] adapter vendor=nvidia architecture=lovelace device= description= isFallbackAdapter=false software=false`, limits `maxBufferSize=4294967292 maxStorageBufferBindingSize=2147483644 maxStorageBuffersPerShaderStage=16 maxComputeWorkgroupsPerDimension=65535`; `[browser] import.meta.env GRAPHTY_GPU_REQUIRE=nvidia GRAPHTY_BROWSER_GPU=nvidia GRAPHTY_NOISE_FLOOR_WRITE=`; vitest `Duration 787ms` (run 1: `Duration 794ms`). Measured 2026-09-15 (`tmp/g0/browser-nvidia.{out,err}`), twice: identical counts and adapter line in the first run (`tmp/g0/run1-no-egl/browser-nvidia.out`)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| L4  | `browser` project on SwiftShader                                                    | `GRAPHTY_BROWSER_GPU=swiftshader GRAPHTY_GPU_REQUIRE=any node scripts/run-browser-project.js`                                                                                                                                                                    | 0         | `numTotalTests=4 numPassedTests=4 numFailedTests=0`; `vitest exited 0` (no exit 124); the test's adapter line: `[browser] adapter vendor=google architecture=swiftshader device= description= isFallbackAdapter=true software=true`, limits `maxBufferSize=1073741824 maxStorageBufferBindingSize=1073741824 maxStorageBuffersPerShaderStage=10 maxComputeWorkgroupsPerDimension=65535`; `[browser] import.meta.env GRAPHTY_GPU_REQUIRE=any GRAPHTY_BROWSER_GPU=swiftshader GRAPHTY_NOISE_FLOOR_WRITE=`; vitest `Duration 642ms` (run 1: `Duration 549ms`). Measured 2026-09-15 (`tmp/g0/browser-swiftshader.{out,err}`), twice: identical counts and adapter line in the first run (`tmp/g0/run1-no-egl/browser-swiftshader.out`)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| P5  | `gpu-report.js` under the vendor policy on NVIDIA                                   | `LD_LIBRARY_PATH=/home/apowers/Projects/webgpu-graph-algorithms/tmp/egl/root/usr/lib/x86_64-linux-gnu GRAPHTY_GPU_REQUIRE=nvidia node scripts/gpu-report.js` (libEGL tree required, as for L1)                                                                   | 0         | `ok: true`; `policy: { level: "vendor", vendor: "nvidia", raw: "nvidia" }`; `adapter.vendor: "nvidia"`, `adapter.architecture: "lovelace"`, `adapter.device: "nvidia-geforce-rtx-4070-super"`, `adapter.description: "NVIDIA: 580.173.02 580.173.2.0"`, `adapter.software: false`; `subgroupMinSize / subgroupMaxSize: 32 / 32`; `features` (18): bgra8unorm-storage, clip-distances, core-features-and-limits, depth-clip-control, depth32float-stencil8, dual-source-blending, float32-blendable, float32-filterable, indirect-first-instance, primitive-index, rg11b10ufloat-renderable, subgroups, texture-component-swizzle, texture-compression-bc, texture-compression-bc-sliced-3d, texture-formats-tier1, texture-formats-tier2, timestamp-query; `adapter.limits` = `deviceLimits` = `{ maxBufferSize: 1099511627776, maxStorageBufferBindingSize: 2147483644, maxStorageBuffersPerShaderStage: 16, maxComputeWorkgroupsPerDimension: 65535 }` (Dawn granted the adapter's maxima); `roundTripMs: 0.04999250000000188`; `runnerClass: "nvidia-lovelace-driver580"`; `nvidiaSmi: { available: true, samples: 10, maxUtilization: 1, maxMemoryUsedMiB: 16, minMemoryUsedMiB: 16 }`; `webgpu: "0.4.0"`, `node: "v22.22.1"`, `reason: null`; stderr `[gpu-report] dawn flags: []`. Measured 2026-09-15 (`tmp/g0/report-nvidia.{out,err}`) |
| P6  | `gpu-report.js` under the vendor policy on lavapipe (the local twin of the red run) | `GRAPHTY_GPU_ADAPTER=llvmpipe GRAPHTY_GPU_REQUIRE=nvidia VK_DRIVER_FILES=/usr/share/vulkan/icd.d/lvp_icd.x86_64.json node scripts/gpu-report.js`                                                                                                                 | 2         | `ok: false`; `policy: { level: "vendor", vendor: "nvidia", raw: "nvidia" }`; `adapter.vendor: "mesa"`, `adapter.architecture: "software"`, `adapter.description: "llvmpipe: Mesa 23.2.1-1ubuntu3.1~22.04.3 (LLVM 15.0.7) 0.0.1"`, `adapter.software: true`, subgroups 8 / 8; `runnerClass: "mesa-software-driver23"`; `reason: "E_NO_ADAPTER: adapter vendor \"mesa\" (mesa/software) does not match GRAPHTY_GPU_REQUIRE=nvidia"`; stderr `[gpu-report] dawn flags: ["adapter=llvmpipe"]`; the JSON document was still printed in full (adapter facts, `deviceLimits`, `roundTripMs: 0.02251900000000262`, the nvidia-smi sample) before the exit. Measured 2026-09-15 (`tmp/g0/report-llvmpipe-nvidia.{out,err}`), twice: identical reason and exit in the first run                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |

Logs: `packages/webgpu-graph-algorithms/tmp/g0/<leg>.out` / `.err` (gitignored; kept until G1).

Note on L1 / P5 (measured 2026-09-15, first run, logs in `tmp/g0/run1-no-egl/`): with `LD_LIBRARY_PATH` unset
(the plan's original L1 / P5 command lines) Dawn-in-Node does NOT see the RTX 4070 SUPER on this box: the Vulkan
loader logs `Warning: loader_scanned_icd_add: Could not get 'vkCreateInstance' via 'vk_icdGetInstanceProcAddr'
for ICD libGLX_nvidia.so.0` (the NVIDIA ICD dlopen()s `libEGL.so.1`, which the container lacks,
`docs/HEADLESS_GPU_REPORT.md`), Dawn silently picks llvmpipe, and the policy caught it exactly as designed:
L1 exited 1 with every GPU test failing on `GRAPHTY_GPU_REQUIRE=hardware: E_SOFTWARE_ONLY: adapter mesa/software
is a software adapter but GRAPHTY_GPU_REQUIRE=hardware` (no silent pass), and P5 exited 2 with `ok: false` and
`reason: "E_NO_ADAPTER: adapter vendor \"mesa\" (mesa/software) does not match GRAPHTY_GPU_REQUIRE=nvidia"`.
The two legs were re-run with the libEGL tree on `LD_LIBRARY_PATH` (the same recipe P0-T2 / P0-T3 used for their
NVIDIA runs); the rows above record that second run. L2, L3, L4 and P6 were identical in both runs.

## 4. The default lane on GitHub (`ci.yml`, `ubuntu-latest`)

| Id  | Fact                                                                                | Value                                                                                              |
| --- | ----------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------- |
| D1  | The P0 PR run (event `pull_request`)                                                | [[fill: run URL, conclusion, created / updated timestamps]]                                        |
| D2  | The `master` run after the merge (event `push`)                                     | [[fill: run URL, conclusion]]                                                                      |
| D3  | Runner image (from the "Set up job" log: Image / Version lines)                     | [[fill: e.g. ubuntu-24.04 / 2025xxxx.x.x]]                                                         |
| D4  | lavapipe adapter line printed by the `node` project (`test/setup/gpu.ts` beforeAll) | [[fill: vendor, architecture, description (the Mesa version on the runner), limits]]               |
| D5  | SwiftShader adapter in the browser smoke (`webgpu-check`)                           | [[fill: numTotalTests / numPassedTests / numFailedTests; the adapter line if the test prints one]] |
| D6  | Step durations: node suite, browser smoke, whole job                                | [[fill: mm:ss each; T-12 budget for the whole lane is 15 min]]                                     |
| D7  | Did the browser step exit 124 (the `browser.close()` hang) on this lane?            | [[fill: yes / no; if yes, numFailedTests must be 0 and the step green]]                            |

## 5. The GPU lane (`gpu.yml`, `runs-on: gpu-linux-t4`, the NVIDIA partner image)

| Id  | Fact                                                                                                                                                                           | Value                                                                                                                                          |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------- |
| M1  | The `master` run after the P0 PR merged (event `push`)                                                                                                                         | [[fill: run URL, conclusion, queue wait (created -> job started), total duration]]                                                             |
| M2  | The image-probe dispatch (event `workflow_dispatch`, branch `g0-image-probe`)                                                                                                  | [[fill: run URL, conclusion, total duration]]                                                                                                  |
| F1  | Ubuntu release (`/etc/os-release` PRETTY_NAME / VERSION_ID)                                                                                                                    | [[fill: PRETTY_NAME / VERSION_ID]]                                                                                                             |
| F2  | glibc (`ldd --version`) -- the `webgpu@0.4.0` pin is lifted only at P-ENV (D11) whatever this says                                                                             | [[fill: the ldd --version first line]]                                                                                                         |
| F3  | Kernel (`uname -r`)                                                                                                                                                            | [[fill: uname -r]]                                                                                                                             |
| F4  | NVIDIA driver and GPU (`nvidia-smi` in the `Driver up` step: Driver Version, CUDA Version, GPU name)                                                                           | [[fill: Driver Version, CUDA Version, GPU name]]                                                                                               |
| F5  | Was `sudo modprobe nvidia nvidia_uvm` needed? (modules before the step: probe output "nvidia modules before Driver up"; after: "Modules after Driver up")                      | [[fill: "needed: no nvidia module before, loaded after" / "no-op: modules already loaded at boot"]]                                            |
| F6  | `libegl1` on the image (`dpkg-query`) and `libEGL.so.1` in the ldconfig cache                                                                                                  | [[fill: package version + status, or "absent"]] -> `GRAPHTY_EGL_LIB_DIR` / `LD_LIBRARY_PATH` stay [[fill: unset / needed]] on the lane         |
| F7  | Mesa on the image (`mesa-vulkan-drivers`, `libvulkan1`, the ICD directory listing) -- the software fallback `GRAPHTY_GPU_REQUIRE=nvidia` guards against                        | [[fill: the two dpkg-query lines (or the absent line) and the ICD directory listing]]                                                          |
| F8  | Did headless Chromium find the T4 WITHOUT Xvfb? (the `Browser smoke on NVIDIA` step of M1 / M2: green with `webgpu-check` asserting vendor nvidia and isFallbackAdapter false) | [[fill: yes / no; if no: the variant-B dispatch (step 14) with xvfb-run -a and its result, and the follow-up commit that adopted the wrapper]] |
| F9  | `xvfb-run` present on the image; `DISPLAY` value                                                                                                                               | [[fill: xvfb-run path or absent; DISPLAY=...]]                                                                                                 |
| F10 | Dawn adapter on the T4 (`gpu-report.json`: vendor, architecture, device, description)                                                                                          | [[fill: vendor, architecture, device, description]]                                                                                            |
| F11 | Subgroup sizes (`subgroupMinSize` / `subgroupMaxSize`)                                                                                                                         | [[fill: subgroupMinSize / subgroupMaxSize]]                                                                                                    |
| F12 | Features granted (`adapter.features`)                                                                                                                                          | [[fill: the features array verbatim]]                                                                                                          |
| F13 | Device limits (`deviceLimits`: maxBufferSize, maxStorageBufferBindingSize, maxStorageBuffersPerShaderStage, maxComputeWorkgroupsPerDimension)                                  | [[fill: the four limits with their values]]                                                                                                    |
| F14 | 4-byte round trip (`roundTripMs`, median of 10)                                                                                                                                | [[fill: roundTripMs]] ms                                                                                                                       |
| F15 | `runnerClass` in the report                                                                                                                                                    | [[fill: must be gpu-linux-t4 (GRAPHTY_RUNNER_CLASS, contract 6.4)]]                                                                            |
| F16 | `nvidiaSmi` sample (available, maxUtilization, maxMemoryUsedMiB)                                                                                                               | [[fill: available, maxUtilization, maxMemoryUsedMiB]]                                                                                          |
| F17 | graph-format canary line (`[gpu-upload] adapter vendor=nvidia ...`)                                                                                                            | [[fill: the line verbatim]]                                                                                                                    |
| F18 | Browser smoke summary on the T4 (numTotalTests / numPassedTests / numFailedTests; exit 124 or not)                                                                             | [[fill: numTotalTests / numPassedTests / numFailedTests; exit 124 or not]]                                                                     |
| F19 | Job duration and cost (minutes x $0.052)                                                                                                                                       | [[fill: mm:ss and the dollar amount]]                                                                                                          |
| F20 | Runner image line(s) from the "Set up job" log (image name / version, runner group)                                                                                            | [[fill: the Set up job image / version / runner group lines]]                                                                                  |
| F21 | `webgpu` / `node` versions in the report                                                                                                                                       | [[fill: webgpu and node from the report]]                                                                                                      |

## 6. The deliberate red run (spec 12.4 item 5; branch `g0-red-run`, `ubuntu-latest` + lavapipe, `GRAPHTY_GPU_REQUIRE=nvidia`)

| Id  | Fact                                                                                                                                                          | Value                                                                                                                   |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------- |
| R1  | Run                                                                                                                                                           | [[fill: run URL, event workflow_dispatch, branch g0-red-run, conclusion failure]]                                       |
| R2  | The failing step                                                                                                                                              | [[fill: "Adapter report (fails loudly on a software adapter; samples GPU utilisation for 10 s)" -- conclusion failure]] |
| R3  | The step's exit code (the log's "Process completed with exit code N.")                                                                                        | [[fill: 2]]                                                                                                             |
| R4  | `gpu-report.json` from the `gpu-results-<run_id>` artifact: `ok`, `policy`, `adapter.vendor`, `adapter.architecture`, `adapter.software`, `reason`            | [[fill: verbatim]]                                                                                                      |
| R5  | Steps after the report: skipped (canary, node projects, no-subgroups pass, Playwright, browser, bench, compare); the artifact upload ran (`if: !cancelled()`) | [[fill: the step list with conclusions]]                                                                                |
| R6  | No `tee` on the report step (the exit code reaches the step)                                                                                                  | [[fill: output of `grep -n "gpu-report.js" .github/workflows/gpu.yml` on master, showing `>` and `&&` and no `\| tee`]] |

## 7. Vitest 3 browser-mode facts (contract 8, 9 item 4)

| Id  | Fact                                                                                                                                                                                    | Value                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| --- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| V1  | The per-instance Playwright `launch` spelling is recorded in `CLAUDE.md` and matches the installed `@vitest/browser` 3.2.7 declaration; the installed Playwright and its Chromium build | `CLAUDE.md` (Verified Platform Facts, the launch-spelling row): `instances: [{ browser: "chromium", launch: { args, env } }]`, `browser.fileParallelism: false` (the "Testing" section spells it out in full with `BrowserInstanceOption extends BrowserProviderOptions`); `node_modules/@vitest/browser/package.json` `"version": "3.2.7"`; `grep -n launch node_modules/@vitest/browser/providers/playwright.d.ts` -> `17:    launch?: LaunchOptions` (inside `declare module 'vitest/node' { export interface BrowserProviderOptions { ... } }`); `node_modules/vitest/dist/chunks/reporters.d.BuRON0I0.d.ts`: line 2434 `instances?: BrowserInstanceOption[];`, line 1761 `fileParallelism?: boolean;`, and line 2347 lists `fileParallelism` among `NonProjectOptions` (root-level only, so `browser.fileParallelism` is the per-project spelling); same as 3.2.4: the 3.2.7 `providers/playwright.d.ts` is byte-identical to the 3.2.4 copy at `~/Projects/pupt-monorepo/node_modules/.pnpm/@vitest+browser@3.2.4_*/` (`diff` empty), and `vitest.config.ts` type-checks against it (B1 lint green); Playwright 1.63.0 (resolved for `^1.54.1`; `playwright-core` 1.63.0), `browsers.json` chromium revision 1243 (`browserVersion` 153.0.8010.12, Chrome for Testing; `chromium-headless-shell` revision 1243 too) (present as `~/.cache/ms-playwright/chromium-1243` and `chromium_headless_shell-1243`; the cache also holds builds 1186-1234 from other projects). `playwright-core/browsers.json` is not exported by the package's `exports`, so it was read at `packages/node_modules/.pnpm/playwright@1.63.0/node_modules/playwright-core/browsers.json`. Measured 2026-09-15 |
| E1  | `test.env` versus `define`: does `test.env` alone put `GRAPHTY_BROWSER_GPU` into `import.meta.env` of the browser tester?                                                               | A: yes -- define is redundant (vitest 3.2.7 / @vitest/browser 3.2.7 / Playwright 1.63.0 Chromium 1243, measured 2026-09-15 with `tmp/g0/no-define.mjs`)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| E2  | The probe's printed line (`G0-ENV-PROBE {...}`) and the probe test's result with the `define` block removed                                                                             | `G0-ENV-PROBE {"GRAPHTY_BROWSER_GPU":"nvidia","GRAPHTY_GPU_REQUIRE":"nvidia"}`; `numTotalTests: 1, numPassedTests: 1, numFailedTests: 0`, vitest exit 0 (no exit 124) -- pass (`tmp/g0/env-probe.{out,err}`, `env-probe-results.json`). Control run (same scratch config plus one `test.env` key that exists nowhere else, `G0_ONLY_IN_TEST_ENV: "from-test-env"`, and a shell-only variable `G0_SHELL_ONLY=shell-value` on the command line): `G0-ENV-CONTROL {"G0_ONLY_IN_TEST_ENV":"from-test-env","GRAPHTY_BROWSER_GPU":"nvidia","keys":["BASE_URL","DEV","G0_ONLY_IN_TEST_ENV","GRAPHTY_BROWSER_GPU","GRAPHTY_GPU_REQUIRE","GRAPHTY_NOISE_FLOOR_WRITE","MODE","PROD","SSR"]}` -- pass: the tester's `import.meta.env` holds exactly the `test.env` keys plus Vite's five built-ins, and the shell-only variable is absent (`tmp/g0/env-probe-control.{out,err}`)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| E3  | Consequence                                                                                                                                                                             | A: a follow-up commit (after G0, before P1-T1 starts) removes the browser project's `define` block from `vitest.config.ts` and keeps `env` (row U2). Until that commit both paths stay in place; they carry the same values, so nothing is wrong, only redundant                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |

## 8. The 7.2 sign-off (D21) and the four contract corrections (contract 9 items 1, 2, 7, 8)

| Id  | Item                                                                                                                         | Outcome                                                                                                                                                                      |
| --- | ---------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| S1  | The sign-off comment on the P0 PR                                                                                            | [[fill: comment URL, date]]                                                                                                                                                  |
| S2  | Item 1 -- gravity centre in `compat: "networkx"` is the ORIGIN (`GRAVITY_CENTER = 1`); paper mode keeps the centroid default | [[fill: ACCEPTED / RE-DECIDED: <text>]]                                                                                                                                      |
| S3  | Item 2 -- per-node swing in `SWING_MODE = 1` is `m_i \|F_i\|`; the global sums stay position-mixed and accumulated           | [[fill: ACCEPTED / RE-DECIDED: <text>]]                                                                                                                                      |
| S4  | Item 7 -- subgroup scratch sized by `SUBGROUP_MIN`; `SUBGROUP_MAX` declared, unused                                          | [[fill: ACCEPTED / RE-DECIDED: <text>]]                                                                                                                                      |
| S5  | Item 8 -- `USE_PERM` / `perm` = the `degreeOrder()` row permutation; `USE_ARC_PERM` reserved for the arc guard               | [[fill: ACCEPTED / RE-DECIDED: <text>]]                                                                                                                                      |
| S6  | The Review-log appendix in `design/webgpu-acceleration-plan.md`                                                              | [[fill: the appendix's first line verbatim]] (the commit that carries it is the one `git log -1 --format=%h -- packages/webgpu-graph-algorithms/docs/decisions/G0.md` names) |
| S7  | The red-run comment on the P0 PR (R1 URL, the failing step, the reason)                                                      | [[fill: comment URL]]                                                                                                                                                        |
| S8  | Re-decisions that change a P1-P3 plan (none when every item is ACCEPTED)                                                     | [[fill: "none" / the list: item -> contract section -> task affected]]                                                                                                       |

## 9. Benchmark baseline for the T4 class

No baseline yet. `benchmarks/run.ts` is the P0 stub (prints "no benchmark groups until P1", writes
nothing) and `scripts/bench-compare.js` printed "nothing to compare" on M1 / M2 (contract 2.9 delta 4).
P1-T7 commits `benchmarks/results/gpu-linux-t4.json` from the `benchmarks/out/gpu-linux-t4.json` of
the first green P1 GPU run (its `gpu-results-<run_id>` artifact); `docs/decisions/G1.md` records it.
The dev box's own class file (`nvidia-ada-lovelace-driver580.json` per contract 6.4, the exact name
from `runnerClass` in P5 above: `nvidia-lovelace-driver580` -- Dawn's architecture string is `lovelace`, not
`ada-lovelace`, so the file is `benchmarks/results/nvidia-lovelace-driver580.json`) is also P1-T7's.

## 10. Follow-ups opened by G0 (each is an owner-approved commit on `master` after this record)

| Id  | Follow-up                                                                                                                                                                                                                 | Trigger | Status                                                                                                                                                 |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------- | ------------------------------------------------------------------------------------------------------------------------------------------------------ |
| U1  | Wrap the `Browser smoke on NVIDIA` step of `gpu.yml` in `xvfb-run -a` (+ `sudo apt-get update && sudo apt-get install -y --no-install-recommends xvfb`, the same `update && install` pairing as `ci.yml`'s lavapipe step) | F8 = no | [[fill: not needed / commit hash]]                                                                                                                     |
| U2  | Remove the browser project's `define` block from `vitest.config.ts`                                                                                                                                                       | E1 = A  | needed (E1 = A): [[fill: the hash of the owner's follow-up commit on master that removes the `define` block and keeps `env` (after G0, before P1-T1)]] |
| U3  | Amend the spec sentences of the four corrections (the Review-log appendix is the amendment; the body text of 7.2 / 3.5 / 4.1 is edited only if the owner prefers in-place edits)                                          | S2-S5   | [[fill: appendix only / in-place edits in commit hash]]                                                                                                |
| U4  | Add the monorepo to the `gpu` runner group at W1 (spec 12.2)                                                                                                                                                              | W1      | open until P10                                                                                                                                         |

## 11. Sign-off

Gate G0 is [[fill: CLOSED / OPEN]] on [[fill: date]] by [[fill: owner]]. P1 starts only when every
row of section 1 reads PASS (G0-18 reads "recorded").
