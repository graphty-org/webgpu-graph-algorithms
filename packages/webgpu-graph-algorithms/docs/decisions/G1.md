# G1 -- the walking skeleton gate (spec 13 row P1; contract 7.2)

Recorded by: P1-T7 (the measurements below were run on 2026-09-15; the owner signs section 7). Commits: the three of
`tmp/commit-p1.sh` (short hashes to be filled in once committed).
Environment: contract section 8 (Node 22.22.1, pnpm 10.0.0, vitest 3.2.7, webgpu 0.4.0, Ubuntu 22.04, driver 580.173.02,
Playwright chromium-1181). Every command ran from `packages/webgpu-graph-algorithms`; every NVIDIA command carried
`LD_LIBRARY_PATH=/home/apowers/Projects/webgpu-graph-algorithms/tmp/egl/root/usr/lib/x86_64-linux-gnu` (the extracted
libEGL tree, contract 8; without it Dawn silently lists only llvmpipe).

## 1. Adapters exercised

| Adapter                         | Runtime        | adapter class (noise floor)  | runner class (benchmarks) | subgroups   | how it was run                                                                                                                                                             |
| ------------------------------- | -------------- | ---------------------------- | ------------------------- | ----------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| NVIDIA RTX 4070 SUPER, Dawn     | node           | nvidia-lovelace-node         | nvidia-lovelace-driver580 | 32 / 32     | `LD_LIBRARY_PATH=<egl tree> GRAPHTY_GPU_REQUIRE=hardware XDG_RUNTIME_DIR=/tmp pnpm exec vitest run --project=node`                                                         |
| Mesa lavapipe, Dawn             | node           | mesa-software-node           | mesa-software-driver23    | 8 / 8       | the default-lane env (`GRAPHTY_GPU_ADAPTER=llvmpipe GRAPHTY_GPU_REQUIRE=any VK_DRIVER_FILES=/usr/share/vulkan/icd.d/lvp_icd.x86_64.json XDG_RUNTIME_DIR=/tmp`)             |
| Chromium SwiftShader            | browser        | google-swiftshader-browser   | --                        | 4 / 4       | `GRAPHTY_BROWSER_GPU=swiftshader GRAPHTY_GPU_REQUIRE=any XDG_RUNTIME_DIR=/tmp node scripts/run-browser-project.js`                                                         |
| Chromium on the RTX 4070 SUPER  | browser        | nvidia-lovelace-browser      | --                        | 32 / 32     | `GRAPHTY_BROWSER_GPU=nvidia GRAPHTY_GPU_REQUIRE=nvidia GRAPHTY_EGL_LIB_DIR=<egl tree> LD_LIBRARY_PATH=<egl tree> XDG_RUNTIME_DIR=/tmp node scripts/run-browser-project.js` |
| GitHub gpu-linux-t4 (NVIDIA T4) | node + browser | not yet run with the P1 tree | gpu-linux-t4              | not yet run | gpu.yml: the first green GPU-lane run after the P1 commits (Step 39 of the P1-T7 plan; owner)                                                                              |

Both Dawn-node and Chromium spell the RTX 4070 SUPER's architecture `lovelace` (not `ada-lovelace` as contract 6.4 / 6.6
assumed for the dev-box file name), so the noise-floor classes are `nvidia-lovelace-node` / `nvidia-lovelace-browser` and
the benchmark runner class is `nvidia-lovelace-driver580`; every file is named through the one-copy rules
(`test/helpers/noise-floor.ts adapterClass()`, `scripts/runner-class.js`), never by hand.

## 2. The G1 checklist (contract 7.2), each item mapped to its evidence

| #   | Item                                                                                                                    | Evidence                                                                                                                                                                                                                                                                                                                                                         | Result                                                                                                                                                                                                                                                                                                                                                                                        | Status        |
| --- | ----------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------- |
| 1   | The 11.5 skeleton on lavapipe, SwiftShader and NVIDIA                                                                   | `test/layouts/skeleton.test.ts` (node: NVIDIA and lavapipe), `test/browser/skeleton.test.ts` (SwiftShader and NVIDIA-Chromium)                                                                                                                                                                                                                                   | node: 14 passed / 14 passed; browser: 5 passed / 5 passed                                                                                                                                                                                                                                                                                                                                     | pass          |
| 2   | u32 results bitwise across adapters: `degree` and the 17M-item map                                                      | the `degree / random1k` pairs of `test/noise-floor.test.ts` (maxAbsError 0 for every pair of the four classes, section 4); `LINEAR_ID_CHECKSUM` = 877493955 reproduced on lavapipe / NVIDIA (`test/kernel/linear-id.test.ts`, 4 passed each) and on SwiftShader / NVIDIA-Chromium (`test/browser/skeleton.test.ts`, `[skeleton] ... checksum 877493955` on both) | 6 degree pairs at 0; the checksum equal on all four adapters                                                                                                                                                                                                                                                                                                                                  | pass          |
| 3   | The FA2 iteration within 1e-5 across adapters, bitwise per adapter                                                      | the `fa2-skeleton.force.cross` row (section 4) and every cross pair of section 4; the "twice bitwise" assertions of both skeleton tests (force, state words, trace)                                                                                                                                                                                              | max cross-adapter maxRelError 3.295e-7 (force), 8.535e-8 (trace); every adapter bitwise across two runs                                                                                                                                                                                                                                                                                       | pass          |
| 4   | The upload-contract tests                                                                                               | `test/memory/residency.test.ts` on NVIDIA and lavapipe                                                                                                                                                                                                                                                                                                           | 20 passed each                                                                                                                                                                                                                                                                                                                                                                                | pass          |
| 5   | The bundle specifier test hard-failing under CI                                                                         | `rm -rf dist && CI=1 pnpm exec vitest run --project=node test/build-output.test.ts` -> red (exit 1); with `dist/` -> green                                                                                                                                                                                                                                       | red run's line: `dist bundle absent under CI: ./dist/webgpu-graph-algorithms.js, ./dist/browser.js, ./dist/node.js -- run pnpm run build:all: expected false to be true` (2 failed, 9 passed, 5 skipped); green run: 15 passed, 1 skipped (the CI-only presence test)                                                                                                                         | pass          |
| 6   | T-1 / T-2 / T-3 in `benchmarks/results/`                                                                                | section 3                                                                                                                                                                                                                                                                                                                                                        | three of four targets met; the 1M/10M upload misses (section 7, owner decision)                                                                                                                                                                                                                                                                                                               | see section 7 |
| 7   | Coverage >= 80 / 80 / 75 / 80 on `--project=node`                                                                       | section 5                                                                                                                                                                                                                                                                                                                                                        | 97.83 / 100 / 96.17 / 97.83                                                                                                                                                                                                                                                                                                                                                                   | pass          |
| 8   | The noise-floor file with degree, reduce and FA2-skeleton rows from all three adapters (SwiftShader through the bridge) | section 4; the ten `*-browser.json` fixtures under `test/fixtures/noise/` (degree, reduce, fill, fa2-repulsion-exact, fa2-speed-finalize x google-swiftshader-browser, nvidia-lovelace-browser)                                                                                                                                                                  | 13 rows, 4 adapters, 6 tolerances; 31 fixture files                                                                                                                                                                                                                                                                                                                                           | pass          |
| 9   | `degree`, `reduce`, K3 and K4 sabotage mutations failing their tests by >= 10x (`coverage.test.ts` green)               | `test/sabotage/degree.test.ts`, `reduce.test.ts`, `fa2-skeleton.test.ts`, `coverage.test.ts` on lavapipe (and NVIDIA)                                                                                                                                                                                                                                            | smallest observed factor per kernel: degree Infinity (exact u32 checks: every mutation produces a wrong value, e.g. `last-row-skipped` karate[33] 0 vs 17), reduce Infinity (exact checks, e.g. `final-writes-out-zero` sum/u32/255@1 3735928559 vs 16566), K3 1.13e4 (`gravity-sign-flipped`, leg paper-prev), K4 1.81e5 (`efficiency-rise-removed`, leg paper); `coverage.test.ts` 6 passed | pass          |
| 10  | Leak counter 0 after release and dispose; one mapAsync per degree                                                       | `test/leak.test.ts` on NVIDIA and lavapipe                                                                                                                                                                                                                                                                                                                       | 3 passed / 3 passed                                                                                                                                                                                                                                                                                                                                                                           | pass          |
| 11  | The barrel export list pinned (P1)                                                                                      | `test/index.test.ts`; `tsc -p tsconfig.strict-consumer.json`                                                                                                                                                                                                                                                                                                     | 4 passed; clean                                                                                                                                                                                                                                                                                                                                                                               | pass          |

## 3. T-1 / T-2 / T-3 (spec 10.4; `benchmarks/results/nvidia-lovelace-driver580.json`, session 2026-09-15T15:46:55.584Z)

The harness closes every timing with `device.queue.onSubmittedWorkDone()` (contract 6.1), so T-3 includes that await, which the
0.04 ms basis of the spec did not. The session was measured by `XDG_RUNTIME_DIR=/tmp pnpm run bench` (5 timed runs after
one warm-up) with the card idle (`nvidia-smi` 0 % / 2 MiB for three seconds before the run); the owner re-runs it on a
quiet card before committing if the desktop was in use (section 7).

| Id  | Benchmark (group / name)                                             | Target           | Measured median of 5 (ms) | min / max (ms)    | Rate            | Pass           |
| --- | -------------------------------------------------------------------- | ---------------- | ------------------------- | ----------------- | --------------- | -------------- |
| T-1 | upload / residency.core 100k/1M weighted hot prefix (16.4 MB, arena) | <= 10 ms         | 5.938                     | 5.784 / 11.721    | 2.76 GB/s       | yes            |
| T-1 | upload / residency.core 1M/10M weighted hot prefix (164.0 MB, arena) | <= 100 ms        | 127.118                   | 126.514 / 127.319 | 1.29 GB/s       | no (section 7) |
| T-2 | roundtrip / degree + 400 KB readback at 100k (core resident)         | <= 2 ms          | 0.868                     | 0.831 / 1.225     | 115.2 M nodes/s | yes            |
| T-3 | roundtrip / empty submit + 4-byte readU32 round trip                 | <= 0.1 ms (Dawn) | 0.083                     | 0.040 / 0.688     | --              | yes            |

`nvidia-smi` during the `scripts/gpu-report.js` sample: utilisation 2 %, memory 16 MiB flat over the 10 samples (quiet).
`node scripts/bench-compare.js` against the committed baseline: the four rows `ok` at x1.00, "no regression above 3x the
baseline of class nvidia-lovelace-driver580", exit 0.

## 4. Cross-adapter results (spec 11.5, 11.9 items 3-4; `benchmarks/results/noise-floor.json`)

Rows of the committed file after the P1-T7 write runs (the browser fixtures written through the commands bridge, then
`test/noise-floor.test.ts` with `GRAPHTY_NOISE_FLOOR_WRITE=1` on NVIDIA and on lavapipe; every row is the maximum over the
pairs that contribute to its id):

| Row id                                                   | kernel / fixture             | comparison    | a                          | b                                 | maxRelError | maxAbsError | samples |
| -------------------------------------------------------- | ---------------------------- | ------------- | -------------------------- | --------------------------------- | ----------- | ----------- | ------- |
| degree.cross                                             | degree / random1k            | cross-adapter | mesa-software-node         | nvidia-lovelace-node              | 0           | 0           | 1000    |
| reduce.sum.twin                                          | reduce / random1k            | twin          | mesa-software-node         | mesa-software-node/no-subgroups   | 1.583e-7    | 1.221e-4    | 11      |
| reduce.sum.cross                                         | reduce / random1k            | cross-adapter | mesa-software-node         | nvidia-lovelace-node              | 1.583e-7    | 1.221e-4    | 11      |
| fa2-skeleton.force.cross                                 | fa2-repulsion-exact / karate | cross-adapter | google-swiftshader-browser | nvidia-lovelace-browser           | 3.295e-7    | 4.883e-4    | 102     |
| fa2-skeleton.force.twin                                  | fa2-repulsion-exact / karate | twin          | mesa-software-node         | mesa-software-node/no-subgroups   | 0           | 0           | 102     |
| fa2-skeleton.force.oracle-f64                            | fa2-repulsion-exact / karate | oracle-f64    | google-swiftshader-browser | oracle-f64                        | 3.413e-7    | 8.282e-4    | 102     |
| fa2-skeleton.force.oracle-f64.google-swiftshader-browser | fa2-repulsion-exact / karate | oracle-f64    | google-swiftshader-browser | oracle-f64                        | 3.413e-7    | 8.282e-4    | 102     |
| fa2-skeleton.force.oracle-f64.nvidia-lovelace-browser    | fa2-repulsion-exact / karate | oracle-f64    | nvidia-lovelace-browser    | oracle-f64                        | 2.571e-7    | 3.953e-4    | 102     |
| fa2-skeleton.trace.cross                                 | fa2-speed-finalize / karate  | cross-adapter | mesa-software-node         | nvidia-lovelace-node              | 8.535e-8    | 3.125e-2    | 4       |
| fa2-skeleton.trace.twin                                  | fa2-speed-finalize / karate  | twin          | nvidia-lovelace-node       | nvidia-lovelace-node/no-subgroups | 8.532e-8    | 3.125e-2    | 4       |
| fa2-skeleton.trace.oracle-f64                            | fa2-speed-finalize / karate  | oracle-f64    | mesa-software-node         | oracle-f64                        | 1.277e-7    | 4.675e-2    | 4       |
| fa2-skeleton.trace.oracle-f64.google-swiftshader-browser | fa2-speed-finalize / karate  | oracle-f64    | google-swiftshader-browser | oracle-f64                        | 4.233e-8    | 1.550e-2    | 4       |
| fa2-skeleton.trace.oracle-f64.nvidia-lovelace-browser    | fa2-speed-finalize / karate  | oracle-f64    | nvidia-lovelace-browser    | oracle-f64                        | 4.233e-8    | 1.550e-2    | 4       |

Every pair the write runs printed (`[noise-floor] <member> cross <a> vs <b>`): degree / random1k -- all six pairs of the
four classes 0 / 0; reduce / random1k -- google-swiftshader-browser vs nvidia-lovelace-browser 8.108e-8, vs
nvidia-lovelace-node 8.108e-8, vs mesa-software-node 1.583e-7, mesa-software-node vs nvidia-lovelace-browser 1.583e-7,
nvidia-lovelace-browser vs nvidia-lovelace-node 0; fa2-repulsion-exact / karate -- google-swiftshader-browser vs
mesa-software-node 0 (bitwise), nvidia-lovelace-browser vs nvidia-lovelace-node 0 (bitwise), every CPU-vs-NVIDIA pair
3.295e-7; fa2-speed-finalize / karate -- SwiftShader vs both NVIDIA classes 0, mesa vs the others 8.532e-8. NVIDIA-Chromium
is bitwise identical to NVIDIA-Dawn on every member; SwiftShader's force is bitwise identical to lavapipe's.

Tolerances in force after this gate (unchanged by the browser fixtures; every floor was already the maximum):
`fa2-skeleton.force` = 3.413e-6 (basis fa2-skeleton.force.oracle-f64, factor 10 <= 10), `fa2-skeleton.trace` = 1.277e-6
(basis fa2-skeleton.trace.oracle-f64, factor 10), `fa2-skeleton.force.cross` = 3.295e-6, `fa2-skeleton.trace.cross` =
8.535e-7, `fa2-skeleton.force.twin` = 5.960e-7 (a bitwise floor lifted to one ulp), `fa2-skeleton.trace.twin` = 8.532e-7.
The Chromium f32 tile noise against the f64 pair sum (the `oracle-f64` rows): google-swiftshader-browser 3.413e-7,
nvidia-lovelace-browser 2.571e-7 -- both under `fa2-skeleton.force`.

## 5. Coverage (spec 11.8; the default-lane run of Step 35)

| lines | functions | branches                            | statements | threshold         | wall time of `--project=node --coverage` on lavapipe            |
| ----- | --------- | ----------------------------------- | ---------- | ----------------- | --------------------------------------------------------------- |
| 97.83 | 100       | 96.17 (96.16 on the Step 41 re-run) | 97.83      | 80 / 80 / 75 / 80 | 00:04 (vitest Duration 2.94 s; 34 files, 494 passed, 1 skipped) |

The second default-lane pass (`GRAPHTY_GPU_NO_SUBGROUPS=1 ... test/primitives test/layouts`): 2 files, 26 passed.

## 6. Baselines committed

- `benchmarks/results/nvidia-lovelace-driver580.json`: 1 session, 2026-09-15T15:46:55.584Z (section 3). The contract's
  expected name `nvidia-ada-lovelace-driver580.json` does not occur on this box: Dawn reports architecture `lovelace`.
- `benchmarks/results/gpu-linux-t4.json`: not yet -- the GPU lane has not run with the P1 benchmarks (the P1 commits are
  not pushed). The owner downloads the `gpu-results-<run id>` artifact of the first green run and commits the file
  (Step 39 of the P1-T7 plan); no gate item depends on it (G0 recorded "no baseline yet").

## 7. Findings, owner decisions, re-fixed numbers

- FINDING (T-1, 1M/10M): `upload / residency.core 1M/10M weighted hot prefix (164.0 MB, arena)` measured 127.118 ms
  (min 126.514, max 127.319; 1.29 GB/s) against the <= 100 ms target; the 100k/1M tier meets its target at 5.938 ms
  (2.76 GB/s). The timed body is `residency.core(s)` -- the upload plan, `createBuffer` and ONE `queue.writeBuffer` of the
  164 MB hot prefix, which under Dawn-node copies the bytes out of the JS ArrayBuffer into Dawn's staging memory before
  the DMA -- plus the closing `onSubmittedWorkDone()`; the spread of 0.8 ms over five runs says the number is the path,
  not noise. The spec's [X] 5-12 GB/s assumption is not met by this path. DECISION (owner, spec 10.4 rule): re-fix the
  1M/10M target with the measured basis (for example <= 150 ms, 1.2x the measurement, pending a mapped-at-creation
  upload path in `src/memory/residency.ts`, P1-T2's file) OR continue P1 until the path meets 100 ms. Initials / date:
  _______.
- FINDING (file names): the dev-box adapter is `lovelace` under both Dawn-node and Chromium, so the baseline is
  `nvidia-lovelace-driver580.json` and the noise classes are `nvidia-lovelace-node` / `nvidia-lovelace-browser`; the
  contract's `nvidia-ada-lovelace-driver580` (6.4, 6.6, 8) and the CLAUDE.md line "Dawn-node says ada-lovelace" are
  corrected in this task's CLAUDE.md edit; the contract text is the owner's edit (no code keys on the literal).
- No determinism finding: after the write runs every Node fixture (`*-node.json`, `*-no-subgroups.json`, `oracle-f64`)
  was byte-identical to its pre-run copy once re-formatted by prettier (the writers emit one value per line; the committed
  files are prettier-formatted, and `pnpm exec prettier --write` over `test/fixtures/noise/*.json` and
  `benchmarks/results/noise-floor.json` restores the committed form).
- `browser.close()` after GPU work: exit 0 on both adapters through `scripts/run-browser-project.js` (no 600 s timeout,
  no exit 124) on 2026-09-15; the wrapper's exit-124 rule stays in place for the GPU lane.
- Follow-ups against other tasks' files (P1-T7 edits none of them): `packages/.gitignore` (P0-T2) -- not needed:
  `git check-ignore -v` matched all four paths (`benchmarks/out` through the root `.gitignore`'s `**/benchmarks/out/`,
  `gpu-report.json` / `browser-results.json` / `coverage` through `packages/.gitignore`); `test/helpers/graphs.ts` (P1-T2)
  -- not needed: it imports only `@graphty/graph-format` and bundled for Chromium as-is.

Signed off: _______ (owner), 2026-09-__.
